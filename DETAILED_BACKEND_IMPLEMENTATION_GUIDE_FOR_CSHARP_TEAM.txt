================================================================================
                    HƯỚNG DẪN TRIỂN KHAI BACKEND CHI TIẾT 
                         CHO HỆ THỐNG POS FRONTEND
                        (C# ASP.NET CORE WEB API)
================================================================================

📋 MỤC LỤC:
1. TỔNG QUAN HỆ THỐNG
2. CẤU TRÚC DỰ ÁN BACKEND
3. DATABASE SCHEMA VÀ MODELS
4. AUTHENTICATION & AUTHORIZATION
5. API CONTROLLERS & ENDPOINTS
6. DTOs VÀ MAPPING
7. SERVICES VÀ BUSINESS LOGIC
8. REAL-TIME FEATURES (SIGNALR)
9. FILE UPLOAD & STORAGE
10. CACHING VÀ PERFORMANCE
11. CONFIGURATION VÀ DEPLOYMENT
12. TESTING STRATEGY
13. COMPLETE API ENDPOINTS MAPPING
14. FRONTEND-BACKEND INTEGRATION
15. THEME & LOCALIZATION SUPPORT
16. ERROR HANDLING & VALIDATION
17. MODAL SYSTEMS & UI COMPONENTS
18. HOOKS & UTILITIES INTEGRATION
19. PRODUCTION DEPLOYMENT GUIDE
20. TROUBLESHOOTING & MONITORING

================================================================================
1. TỔNG QUAN HỆ THỐNG
================================================================================

Hệ thống POS Frontend yêu cầu backend API với các tính năng chính:

🔹 Authentication & Authorization với JWT
🔹 Role-based access control (Staff, Manager, Admin)  
🔹 Permission-based granular access
🔹 Multi-store support
🔹 Real-time notifications (SignalR)
🔹 File upload (images, documents)
🔹 Pagination và filtering
🔹 Analytics và reporting
🔹 Multi-module: POS, CRM, LRM, HRM

FRONTEND TECH STACK:
- React 18 + TypeScript
- React Router v6
- Context API (Auth, Theme, I18n)
- Tailwind CSS + shadcn/ui
- Date-fns, Recharts, Lucide Icons

BACKEND REQUIREMENTS:
- C# ASP.NET Core 8.0 Web API
- Entity Framework Core
- SQL Server/PostgreSQL
- JWT Authentication
- SignalR cho real-time
- AutoMapper cho DTOs
- Swagger/OpenAPI documentation

================================================================================
2. CẤU TRÚC DỰ ÁN BACKEND
================================================================================

POSSystem.Backend/
├── POSSystem.API/                          # Main Web API project
│   ├── Controllers/                        
│   │   ├── AuthController.cs               # Authentication endpoints
│   │   ├── UsersController.cs              # User management
│   │   ├── StoresController.cs             # Store management
│   │   ├── ProductsController.cs           # Product CRUD
│   │   ├── InventoryController.cs          # Inventory management
│   │   ��── OrdersController.cs             # Order processing
│   │   ├── ShiftsController.cs             # Shift management
│   │   ├── TasksController.cs              # Task management
│   │   ├── AnalyticsController.cs          # Analytics & reports
│   │   ├── CRMController.cs                # Customer management
│   │   ├── LRMController.cs                # Logistics management
│   │   ├── HRMController.cs                # Human resources
│   │   ├── NotificationsController.cs      # Notifications
│   │   └── FilesController.cs              # File upload/download
│   ├── Hubs/                              
│   │   └── NotificationHub.cs              # SignalR hub
│   ├── Middleware/                        
│   │   ├── ExceptionMiddleware.cs          # Global exception handling
│   │   ├── AuthenticationMiddleware.cs     # Custom auth middleware
│   │   └── PermissionMiddleware.cs         # Permission checking
│   ├── Filters/                           
│   │   ├── PermissionFilter.cs             # Permission attribute filter
│   │   └── ValidationFilter.cs             # Model validation filter
│   ├── Extensions/                         
│   │   ├── ServiceCollectionExtensions.cs  # DI configuration
│   │   └── ApplicationBuilderExtensions.cs # Middleware configuration
│   ├── Program.cs                          # Application entry point
│   └── appsettings.json                    # Configuration
├── POSSystem.Core/                         # Domain layer
│   ├── Entities/                          
│   │   ├── User.cs                        
│   │   ├── Store.cs                       
│   │   ├── Product.cs                     
│   │   ├── InventoryItem.cs               
│   │   ├── Order.cs                       
│   │   ├── OrderItem.cs                   
│   │   ├── Shift.cs                       
│   │   ├── Task.cs                        
│   │   ├── Notification.cs                
│   │   ├── Customer.cs                    
│   │   ├── CRMInteraction.cs              
│   │   ├── Supplier.cs                    
│   │   ├── StockTransfer.cs               
│   │   ├── Employee.cs                    
│   │   ├── LeaveRequest.cs                
│   │   └── WorkSchedule.cs                
│   ├── Enums/                             
│   │   ├── UserRole.cs                    
│   │   ├── OrderStatus.cs                 
│   │   ├── PaymentMethod.cs               
│   │   ├── ShiftStatus.cs                 
│   │   ├── TaskStatus.cs                  
│   │   ├── TaskPriority.cs                
│   │   └── LeaveRequestStatus.cs          
│   ├── Interfaces/                        
│   │   ├── IRepository.cs                 # Generic repository
│   │   ├── IUserService.cs                
│   │   ├── IAuthService.cs                
│   │   ├── IProductService.cs             
│   │   ├── IOrderService.cs               
│   │   ├── IInventoryService.cs           
│   │   ├── IShiftService.cs               
│   │   ├── ITaskService.cs                
│   │   ├── IAnalyticsService.cs           
│   │   ├── ICRMService.cs                 
│   │   ├── ILRMService.cs                 
│   │   ├── IHRMService.cs                 
│   │   ├── INotificationService.cs        
│   │   └── IFileStorageService.cs         
│   └── Constants/                         
│       ├── Permissions.cs                 # All permission constants
│       ├── RolePermissions.cs             # Role-permission mapping
│       └── SystemConstants.cs             # System-wide constants
├── POSSystem.Infrastructure/               # Infrastructure layer
│   ��── Data/                             
│   │   ├── ApplicationDbContext.cs        # EF DbContext
│   │   ├── Configurations/                # Entity configurations
│   │   │   ├── UserConfiguration.cs       
│   │   │   ├── ProductConfiguration.cs    
│   │   │   ├── OrderConfiguration.cs      
│   │   │   └── ...                        
│   │   ├── Migrations/                    # EF migrations
│   │   └── Seeds/                         # Initial data seeding
│   │       ├── UserSeeder.cs              
│   │       ├── StoreSeeder.cs             
│   │       └── ProductSeeder.cs           
│   ├── Repositories/                      
│   │   ├── GenericRepository.cs           # Generic repository implementation
│   │   ├── UserRepository.cs              
│   │   ├── ProductRepository.cs           
│   │   ├── OrderRepository.cs             
│   │   └── ...                           
│   ├── Services/                          
│   │   ├── AuthService.cs                 # Authentication logic
│   │   ├── JwtTokenService.cs             # JWT token management
│   │   ├── UserService.cs                 
│   │   ├── ProductService.cs              
│   │   ├── OrderService.cs                
│   │   ├── InventoryService.cs            
│   │   ├── ShiftService.cs                
│   │   ├── TaskService.cs                 
│   │   ├── AnalyticsService.cs            
│   │   ├── CRMService.cs                  
│   │   ├── LRMService.cs                  
│   │   ├── HRMService.cs                  
│   │   ├── NotificationService.cs         
│   │   ├── FileStorageService.cs          # File upload/storage
│   │   ├── EmailService.cs                # Email notifications
│   │   └── ReportService.cs               # Report generation
│   └── Extensions/                        
│       ├── QueryExtensions.cs             # LINQ extensions
│       └── EntityExtensions.cs            # Entity helper methods
├── POSSystem.Application/                  # Application layer
│   ├── DTOs/                             
│   │   ├── Auth/                         
│   │   │   ├── LoginRequestDto.cs         
│   │   │   ├── LoginResponseDto.cs        
│   │   │   ├── RegisterRequestDto.cs      
│   │   │   └── UserDto.cs                 
│   │   ├── Products/                      
│   │   │   ├── ProductDto.cs              
│   │   │   ├── CreateProductDto.cs        
│   │   │   ├── UpdateProductDto.cs        
│   │   │   └── ProductWithInventoryDto.cs 
│   │   ├── Orders/                        
│   │   │   ├── OrderDto.cs                
│   │   │   ├── CreateOrderDto.cs          
│   │   │   ├── UpdateOrderDto.cs          
│   │   │   ├── OrderItemDto.cs            
│   │   │   └── OrderSummaryDto.cs         
│   │   ├── Inventory/                     
│   │   │   ├── InventoryItemDto.cs        
│   │   │   ├── StockAdjustmentDto.cs      
│   │   │   └── InventoryReportDto.cs      
│   │   ├── Analytics/                     
│   │   │   ├── AnalyticsDto.cs            
│   │   │   ├── RevenueReportDto.cs        
│   │   │   ├── SalesReportDto.cs          
│   │   │   └── PerformanceReportDto.cs    
│   │   ├── CRM/                          
│   │   │   ├── CustomerDto.cs             
│   │   │   ├── CRMInteractionDto.cs       
│   │   │   └── CustomerAnalyticsDto.cs    
│   │   ├── LRM/                          
│   │   │   ├── SupplierDto.cs             
│   │   │   ├── StockTransferDto.cs        
│   │   │   └── ShippingStatusDto.cs       
│   │   ├── HRM/                          
│   │   │   ├── EmployeeDto.cs             
│   │   │   ├── LeaveRequestDto.cs         
│   │   │   ├── WorkScheduleDto.cs         
│   │   │   └── PerformanceMetricDto.cs    
│   │   └── Common/                        
│   │       ├── PagedResultDto.cs          # Pagination wrapper
│   │       ├── ApiResponseDto.cs          # Standard API response
│   │       └── NotificationDto.cs         
│   ├── Mappings/                         
│   │   ├── AutoMapperProfile.cs           # AutoMapper configuration
│   │   ├── UserMappingProfile.cs          
│   │   ├── ProductMappingProfile.cs       
│   │   └── ...                           
│   ├── Validators/                        
│   │   ├── CreateProductValidator.cs      # FluentValidation validators
│   │   ├── CreateOrderValidator.cs        
│   │   └── ...                           
│   └── Helpers/                          
│       ├── PaginationHelper.cs            
│       ├── FilterHelper.cs                
│       └── ExpressionHelper.cs            
└── POSSystem.Tests/                        # Test projects
    ├── POSSystem.UnitTests/               
    ├── POSSystem.IntegrationTests/        
    └── POSSystem.E2ETests/                

================================================================================
3. DATABASE SCHEMA VÀ MODELS
================================================================================

3.1. COMPLETE ENTITIES BASED ON FRONTEND TYPES

// BaseEntity.cs
public abstract class BaseEntity
{
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
    public DateTime UpdatedAt { get; set; } = DateTime.UtcNow;
    public string CreatedBy { get; set; } = string.Empty;
    public string? UpdatedBy { get; set; }
    public bool IsDeleted { get; set; } = false;
}

// User.cs
[Table("Users")]
[Index(nameof(Email), IsUnique = true)]
[Index(nameof(StoreId))]
public class User : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [EmailAddress]
    [MaxLength(255)]
    public string Email { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(100)]
    public string Name { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(255)]
    public string PasswordHash { get; set; } = string.Empty;
    
    [Required]
    public UserRole Role { get; set; }
    
    [Required]
    [MaxLength(36)]
    public string StoreId { get; set; } = string.Empty;
    
    [MaxLength(36)]
    public string? ManagerId { get; set; }
    
    [MaxLength(500)]
    public string? Avatar { get; set; }
    
    public bool IsActive { get; set; } = true;
    
    public DateTime? LastLogin { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(StoreId))]
    public Store Store { get; set; } = null!;
    
    [ForeignKey(nameof(ManagerId))]
    public User? Manager { get; set; }
    
    [InverseProperty(nameof(Manager))]
    public ICollection<User> Subordinates { get; set; } = new List<User>();
    
    [InverseProperty(nameof(Order.Staff))]
    public ICollection<Order> Orders { get; set; } = new List<Order>();
    
    [InverseProperty(nameof(Shift.Staff))]
    public ICollection<Shift> Shifts { get; set; } = new List<Shift>();
    
    [InverseProperty(nameof(Task.AssignedToUser))]
    public ICollection<Task> AssignedTasks { get; set; } = new List<Task>();
    
    [InverseProperty(nameof(Task.AssignedByUser))]
    public ICollection<Task> CreatedTasks { get; set; } = new List<Task>();
    
    [InverseProperty(nameof(Notification.User))]
    public ICollection<Notification> Notifications { get; set; } = new List<Notification>();
    
    [InverseProperty(nameof(CRMInteraction.CreatedByUser))]
    public ICollection<CRMInteraction> CRMInteractions { get; set; } = new List<CRMInteraction>();
}

// Store.cs
[Table("Stores")]
[Index(nameof(Name))]
public class Store : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(200)]
    public string Name { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(500)]
    public string Address { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(20)]
    public string Phone { get; set; } = string.Empty;
    
    [Required]
    [EmailAddress]
    [MaxLength(255)]
    public string Email { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(10)]
    public string Currency { get; set; } = "USD";
    
    [Required]
    [MaxLength(50)]
    public string Timezone { get; set; } = "UTC";
    
    public bool IsActive { get; set; } = true;
    
    // Navigation properties
    [InverseProperty(nameof(User.Store))]
    public ICollection<User> Users { get; set; } = new List<User>();
    
    [InverseProperty(nameof(InventoryItem.Store))]
    public ICollection<InventoryItem> Inventory { get; set; } = new List<InventoryItem>();
    
    [InverseProperty(nameof(Order.Store))]
    public ICollection<Order> Orders { get; set; } = new List<Order>();
    
    [InverseProperty(nameof(Shift.Store))]
    public ICollection<Shift> Shifts { get; set; } = new List<Shift>();
    
    [InverseProperty(nameof(Employee.Store))]
    public ICollection<Employee> Employees { get; set; } = new List<Employee>();
}

// Product.cs
[Table("Products")]
[Index(nameof(Barcode), IsUnique = true)]
[Index(nameof(Category))]
[Index(nameof(Name))]
public class Product : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(200)]
    public string Name { get; set; } = string.Empty;
    
    [MaxLength(1000)]
    public string Description { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(50)]
    public string Barcode { get; set; } = string.Empty;
    
    [Required]
    [Precision(10, 2)]
    public decimal Price { get; set; }
    
    [Required]
    [Precision(10, 2)]
    public decimal Cost { get; set; }
    
    [Required]
    [MaxLength(100)]
    public string Category { get; set; } = string.Empty;
    
    [MaxLength(500)]
    public string? Image { get; set; }
    
    public bool IsActive { get; set; } = true;
    
    // Navigation properties
    [InverseProperty(nameof(InventoryItem.Product))]
    public ICollection<InventoryItem> Inventory { get; set; } = new List<InventoryItem>();
    
    [InverseProperty(nameof(OrderItem.Product))]
    public ICollection<OrderItem> OrderItems { get; set; } = new List<OrderItem>();
    
    [InverseProperty(nameof(TransferItem.Product))]
    public ICollection<TransferItem> TransferItems { get; set; } = new List<TransferItem>();
    
    public ICollection<Supplier> Suppliers { get; set; } = new List<Supplier>();
}

// InventoryItem.cs
[Table("InventoryItems")]
[Index(nameof(ProductId), nameof(StoreId), IsUnique = true)]
[Index(nameof(Quantity))]
public class InventoryItem : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string ProductId { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(36)]
    public string StoreId { get; set; } = string.Empty;
    
    [Required]
    public int Quantity { get; set; } = 0;
    
    [Required]
    public int MinStock { get; set; } = 0;
    
    [Required]
    public int MaxStock { get; set; } = 0;
    
    public DateTime LastUpdated { get; set; } = DateTime.UtcNow;
    
    // Navigation properties
    [ForeignKey(nameof(ProductId))]
    public Product Product { get; set; } = null!;
    
    [ForeignKey(nameof(StoreId))]
    public Store Store { get; set; } = null!;
    
    [InverseProperty(nameof(InventoryMovement.InventoryItem))]
    public ICollection<InventoryMovement> Movements { get; set; } = new List<InventoryMovement>();
}

// InventoryMovement.cs
[Table("InventoryMovements")]
[Index(nameof(CreatedAt))]
[Index(nameof(MovementType))]
public class InventoryMovement : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string InventoryItemId { get; set; } = string.Empty;
    
    [Required]
    public InventoryMovementType MovementType { get; set; }
    
    [Required]
    public int Quantity { get; set; }
    
    [Required]
    public int PreviousQuantity { get; set; }
    
    [Required]
    public int NewQuantity { get; set; }
    
    [MaxLength(500)]
    public string? Reason { get; set; }
    
    [MaxLength(36)]
    public string? OrderId { get; set; }
    
    [MaxLength(36)]
    public string? TransferId { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(InventoryItemId))]
    public InventoryItem InventoryItem { get; set; } = null!;
    
    [ForeignKey(nameof(OrderId))]
    public Order? Order { get; set; }
    
    [ForeignKey(nameof(TransferId))]
    public StockTransfer? Transfer { get; set; }
}

// Order.cs
[Table("Orders")]
[Index(nameof(OrderNumber), IsUnique = true)]
[Index(nameof(StoreId), nameof(CreatedAt))]
[Index(nameof(Status))]
[Index(nameof(PaymentStatus))]
public class Order : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(50)]
    public string OrderNumber { get; set; } = string.Empty;
    
    [MaxLength(36)]
    public string? CustomerId { get; set; }
    
    [MaxLength(100)]
    public string? CustomerName { get; set; }
    
    [MaxLength(20)]
    public string? CustomerPhone { get; set; }
    
    [Required]
    [MaxLength(36)]
    public string StoreId { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(36)]
    public string StaffId { get; set; } = string.Empty;
    
    [Required]
    public OrderStatus Status { get; set; } = OrderStatus.Pending;
    
    [Required]
    [Precision(10, 2)]
    public decimal Subtotal { get; set; } = 0;
    
    [Required]
    [Precision(10, 2)]
    public decimal Tax { get; set; } = 0;
    
    [Required]
    [Precision(10, 2)]
    public decimal Discount { get; set; } = 0;
    
    [Required]
    [Precision(10, 2)]
    public decimal Tip { get; set; } = 0;
    
    [Required]
    [Precision(10, 2)]
    public decimal Total { get; set; } = 0;
    
    [Required]
    public PaymentMethod PaymentMethod { get; set; }
    
    [Required]
    [MaxLength(20)]
    public string PaymentStatus { get; set; } = "pending";
    
    [MaxLength(1000)]
    public string? Notes { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(StoreId))]
    public Store Store { get; set; } = null!;
    
    [ForeignKey(nameof(StaffId))]
    public User Staff { get; set; } = null!;
    
    [ForeignKey(nameof(CustomerId))]
    public Customer? Customer { get; set; }
    
    [InverseProperty(nameof(OrderItem.Order))]
    public ICollection<OrderItem> Items { get; set; } = new List<OrderItem>();
    
    [InverseProperty(nameof(ShippingStatus.Order))]
    public ShippingStatus? ShippingStatus { get; set; }
    
    [InverseProperty(nameof(InventoryMovement.Order))]
    public ICollection<InventoryMovement> InventoryMovements { get; set; } = new List<InventoryMovement>();
}

// OrderItem.cs
[Table("OrderItems")]
[Index(nameof(OrderId))]
[Index(nameof(ProductId))]
public class OrderItem : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string OrderId { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(36)]
    public string ProductId { get; set; } = string.Empty;
    
    [Required]
    public int Quantity { get; set; }
    
    [Required]
    [Precision(10, 2)]
    public decimal Price { get; set; }
    
    [Required]
    [Precision(10, 2)]
    public decimal Discount { get; set; } = 0;
    
    [Required]
    [Precision(10, 2)]
    public decimal Total { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(OrderId))]
    public Order Order { get; set; } = null!;
    
    [ForeignKey(nameof(ProductId))]
    public Product Product { get; set; } = null!;
}

// Shift.cs
[Table("Shifts")]
[Index(nameof(StaffId), nameof(StartTime))]
[Index(nameof(Status))]
public class Shift : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string StaffId { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(36)]
    public string StoreId { get; set; } = string.Empty;
    
    [Required]
    public DateTime StartTime { get; set; }
    
    public DateTime? EndTime { get; set; }
    
    [Required]
    [Precision(10, 2)]
    public decimal OpeningCash { get; set; } = 0;
    
    [Precision(10, 2)]
    public decimal? ClosingCash { get; set; }
    
    [Required]
    [Precision(10, 2)]
    public decimal TotalSales { get; set; } = 0;
    
    [Required]
    public int TotalTransactions { get; set; } = 0;
    
    [Required]
    public ShiftStatus Status { get; set; } = ShiftStatus.Open;
    
    [MaxLength(1000)]
    public string? Notes { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(StaffId))]
    public User Staff { get; set; } = null!;
    
    [ForeignKey(nameof(StoreId))]
    public Store Store { get; set; } = null!;
}

// Task.cs
[Table("Tasks")]
[Index(nameof(AssignedTo), nameof(Status))]
[Index(nameof(Priority))]
[Index(nameof(DueDate))]
public class Task : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(200)]
    public string Title { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(1000)]
    public string Description { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(36)]
    public string AssignedTo { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(36)]
    public string AssignedBy { get; set; } = string.Empty;
    
    [Required]
    public TaskPriority Priority { get; set; } = TaskPriority.Medium;
    
    [Required]
    public TaskStatus Status { get; set; } = TaskStatus.Pending;
    
    public DateTime? DueDate { get; set; }
    
    public DateTime? CompletedAt { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(AssignedTo))]
    public User AssignedToUser { get; set; } = null!;
    
    [ForeignKey(nameof(AssignedBy))]
    public User AssignedByUser { get; set; } = null!;
}

// Notification.cs
[Table("Notifications")]
[Index(nameof(UserId), nameof(IsRead))]
[Index(nameof(CreatedAt))]
public class Notification : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string UserId { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(50)]
    public string Type { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(200)]
    public string Title { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(1000)]
    public string Message { get; set; } = string.Empty;
    
    public bool IsRead { get; set; } = false;
    
    [Column(TypeName = "nvarchar(max)")]
    public string? Data { get; set; } // JSON data
    
    // Navigation properties
    [ForeignKey(nameof(UserId))]
    public User User { get; set; } = null!;
}

// Store.cs
public class Store : BaseEntity
{
    public string Id { get; set; } = Guid.NewGuid().ToString();
    public string Name { get; set; }
    public string Address { get; set; }
    public string Phone { get; set; }
    public string Email { get; set; }
    public string Currency { get; set; } = "USD";
    public string Timezone { get; set; } = "UTC";
    public bool IsActive { get; set; } = true;
    
    // Navigation properties
    public ICollection<User> Users { get; set; } = new List<User>();
    public ICollection<InventoryItem> Inventory { get; set; } = new List<InventoryItem>();
    public ICollection<Order> Orders { get; set; } = new List<Order>();
    public ICollection<Shift> Shifts { get; set; } = new List<Shift>();
}

// Product.cs
public class Product : BaseEntity
{
    public string Id { get; set; } = Guid.NewGuid().ToString();
    public string Name { get; set; }
    public string Description { get; set; }
    public string Barcode { get; set; }
    public decimal Price { get; set; }
    public decimal Cost { get; set; }
    public string Category { get; set; }
    public string? Image { get; set; }
    public bool IsActive { get; set; } = true;
    
    // Navigation properties
    public ICollection<InventoryItem> Inventory { get; set; } = new List<InventoryItem>();
    public ICollection<OrderItem> OrderItems { get; set; } = new List<OrderItem>();
}

// Order.cs
public class Order : BaseEntity
{
    public string Id { get; set; } = Guid.NewGuid().ToString();
    public string OrderNumber { get; set; }
    public string? CustomerId { get; set; }
    public string? CustomerName { get; set; }
    public string? CustomerPhone { get; set; }
    public string StoreId { get; set; }
    public string StaffId { get; set; }
    public OrderStatus Status { get; set; }
    public decimal Subtotal { get; set; }
    public decimal Tax { get; set; }
    public decimal Discount { get; set; }
    public decimal Tip { get; set; }
    public decimal Total { get; set; }
    public PaymentMethod PaymentMethod { get; set; }
    public string PaymentStatus { get; set; }
    public string? Notes { get; set; }
    
    // Navigation properties
    public Store Store { get; set; }
    public User Staff { get; set; }
    public Customer? Customer { get; set; }
    public ICollection<OrderItem> Items { get; set; } = new List<OrderItem>();
}

// InventoryItem.cs
public class InventoryItem : BaseEntity
{
    public string Id { get; set; } = Guid.NewGuid().ToString();
    public string ProductId { get; set; }
    public string StoreId { get; set; }
    public int Quantity { get; set; }
    public int MinStock { get; set; }
    public int MaxStock { get; set; }
    public DateTime LastUpdated { get; set; }
    
    // Navigation properties
    public Product Product { get; set; }
    public Store Store { get; set; }
}

3.2. CRM ENTITIES

// Customer.cs
[Table("Customers")]
[Index(nameof(Email))]
[Index(nameof(Phone))]
[Index(nameof(Status))]
public class Customer : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(100)]
    public string Name { get; set; } = string.Empty;
    
    [Required]
    [EmailAddress]
    [MaxLength(255)]
    public string Email { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(20)]
    public string Phone { get; set; } = string.Empty;
    
    [MaxLength(500)]
    public string? Address { get; set; }
    
    [MaxLength(100)]
    public string? Company { get; set; }
    
    [Required]
    public CustomerStatus Status { get; set; } = CustomerStatus.Lead;
    
    public int TotalOrders { get; set; } = 0;
    
    [Precision(10, 2)]
    public decimal TotalSpent { get; set; } = 0;
    
    public DateTime? LastOrderDate { get; set; }
    
    // Navigation properties
    [InverseProperty(nameof(Order.Customer))]
    public ICollection<Order> Orders { get; set; } = new List<Order>();
    
    [InverseProperty(nameof(CRMInteraction.Customer))]
    public ICollection<CRMInteraction> Interactions { get; set; } = new List<CRMInteraction>();
}

// CRMInteraction.cs
[Table("CRMInteractions")]
[Index(nameof(CustomerId), nameof(CreatedAt))]
[Index(nameof(Type))]
[Index(nameof(Status))]
public class CRMInteraction : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string CustomerId { get; set; } = string.Empty;
    
    [Required]
    public CRMInteractionType Type { get; set; }
    
    [Required]
    [MaxLength(200)]
    public string Title { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(2000)]
    public string Description { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(36)]
    public string CreatedBy { get; set; } = string.Empty;
    
    public DateTime? FollowUpDate { get; set; }
    
    [Required]
    public CRMInteractionStatus Status { get; set; } = CRMInteractionStatus.Pending;
    
    // Navigation properties
    [ForeignKey(nameof(CustomerId))]
    public Customer Customer { get; set; } = null!;
    
    [ForeignKey(nameof(CreatedBy))]
    public User CreatedByUser { get; set; } = null!;
}

3.3. LRM (LOGISTICS) ENTITIES

// Supplier.cs
[Table("Suppliers")]
[Index(nameof(Name))]
[Index(nameof(Status))]
public class Supplier : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(200)]
    public string Name { get; set; } = string.Empty;
    
    [Required]
    [EmailAddress]
    [MaxLength(255)]
    public string Email { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(20)]
    public string Phone { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(500)]
    public string Address { get; set; } = string.Empty;
    
    [Required]
    public SupplierStatus Status { get; set; } = SupplierStatus.Active;
    
    [Required]
    [MaxLength(100)]
    public string PaymentTerms { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(100)]
    public string DeliverySchedule { get; set; } = string.Empty;
    
    // Navigation properties
    public ICollection<Product> Products { get; set; } = new List<Product>();
    
    [InverseProperty(nameof(StockTransfer.Supplier))]
    public ICollection<StockTransfer> StockTransfers { get; set; } = new List<StockTransfer>();
}

// StockTransfer.cs
[Table("StockTransfers")]
[Index(nameof(Status))]
[Index(nameof(CreatedAt))]
public class StockTransfer : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(200)]
    public string FromWarehouse { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(200)]
    public string ToWarehouse { get; set; } = string.Empty;
    
    [Required]
    public StockTransferStatus Status { get; set; } = StockTransferStatus.Pending;
    
    [Required]
    [MaxLength(36)]
    public string RequestedBy { get; set; } = string.Empty;
    
    [MaxLength(36)]
    public string? ApprovedBy { get; set; }
    
    [MaxLength(36)]
    public string? SupplierId { get; set; }
    
    public DateTime? DeliveryDate { get; set; }
    
    [MaxLength(1000)]
    public string? Notes { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(RequestedBy))]
    public User RequestedByUser { get; set; } = null!;
    
    [ForeignKey(nameof(ApprovedBy))]
    public User? ApprovedByUser { get; set; }
    
    [ForeignKey(nameof(SupplierId))]
    public Supplier? Supplier { get; set; }
    
    [InverseProperty(nameof(TransferItem.StockTransfer))]
    public ICollection<TransferItem> Items { get; set; } = new List<TransferItem>();
    
    [InverseProperty(nameof(InventoryMovement.Transfer))]
    public ICollection<InventoryMovement> InventoryMovements { get; set; } = new List<InventoryMovement>();
}

// TransferItem.cs
[Table("TransferItems")]
[Index(nameof(StockTransferId))]
[Index(nameof(ProductId))]
public class TransferItem : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string StockTransferId { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(36)]
    public string ProductId { get; set; } = string.Empty;
    
    [Required]
    public int Quantity { get; set; }
    
    public int? Received { get; set; }
    
    [MaxLength(1000)]
    public string? Notes { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(StockTransferId))]
    public StockTransfer StockTransfer { get; set; } = null!;
    
    [ForeignKey(nameof(ProductId))]
    public Product Product { get; set; } = null!;
}

// ShippingStatus.cs
[Table("ShippingStatuses")]
[Index(nameof(OrderId), IsUnique = true)]
[Index(nameof(Status))]
[Index(nameof(TrackingNumber))]
public class ShippingStatus : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string OrderId { get; set; } = string.Empty;
    
    [Required]
    public ShippingStatusType Status { get; set; } = ShippingStatusType.Pending;
    
    [MaxLength(100)]
    public string? TrackingNumber { get; set; }
    
    [MaxLength(100)]
    public string? Carrier { get; set; }
    
    public DateTime? EstimatedDelivery { get; set; }
    
    public DateTime? ActualDelivery { get; set; }
    
    [MaxLength(1000)]
    public string? Notes { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(OrderId))]
    public Order Order { get; set; } = null!;
}

3.4. HRM (HUMAN RESOURCES) ENTITIES

// Employee.cs
[Table("Employees")]
[Index(nameof(Email), IsUnique = true)]
[Index(nameof(Department))]
[Index(nameof(Status))]
public class Employee : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(100)]
    public string Name { get; set; } = string.Empty;
    
    [Required]
    [EmailAddress]
    [MaxLength(255)]
    public string Email { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(20)]
    public string Phone { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(100)]
    public string Position { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(100)]
    public string Department { get; set; } = string.Empty;
    
    [Required]
    public DateTime HireDate { get; set; }
    
    [Required]
    [Precision(10, 2)]
    public decimal Salary { get; set; }
    
    [Required]
    public EmployeeStatus Status { get; set; } = EmployeeStatus.Active;
    
    [MaxLength(36)]
    public string? ManagerId { get; set; }
    
    [MaxLength(36)]
    public string? StoreId { get; set; }
    
    [MaxLength(500)]
    public string? Avatar { get; set; }
    
    [Column(TypeName = "nvarchar(max)")]
    public string Skills { get; set; } = "[]"; // JSON array
    
    // Navigation properties
    [ForeignKey(nameof(ManagerId))]
    public Employee? Manager { get; set; }
    
    [ForeignKey(nameof(StoreId))]
    public Store? Store { get; set; }
    
    [InverseProperty(nameof(Manager))]
    public ICollection<Employee> Subordinates { get; set; } = new List<Employee>();
    
    [InverseProperty(nameof(PerformanceMetric.Employee))]
    public ICollection<PerformanceMetric> Performance { get; set; } = new List<PerformanceMetric>();
    
    [InverseProperty(nameof(LeaveRequest.Employee))]
    public ICollection<LeaveRequest> LeaveRequests { get; set; } = new List<LeaveRequest>();
    
    [InverseProperty(nameof(WorkSchedule.Employee))]
    public ICollection<WorkSchedule> WorkSchedules { get; set; } = new List<WorkSchedule>();
}

// PerformanceMetric.cs
[Table("PerformanceMetrics")]
[Index(nameof(EmployeeId), nameof(Period))]
public class PerformanceMetric : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string EmployeeId { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(50)]
    public string Period { get; set; } = string.Empty; // e.g., "January 2024"
    
    public int OrdersProcessed { get; set; } = 0;
    
    public int TasksCompleted { get; set; } = 0;
    
    [Precision(3, 2)]
    public decimal CustomerRating { get; set; } = 0;
    
    [Precision(10, 2)]
    public decimal Sales { get; set; } = 0;
    
    [MaxLength(1000)]
    public string? Notes { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(EmployeeId))]
    public Employee Employee { get; set; } = null!;
}

// LeaveRequest.cs
[Table("LeaveRequests")]
[Index(nameof(EmployeeId), nameof(Status))]
[Index(nameof(StartDate))]
public class LeaveRequest : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string EmployeeId { get; set; } = string.Empty;
    
    [Required]
    public LeaveRequestType Type { get; set; }
    
    [Required]
    public DateTime StartDate { get; set; }
    
    [Required]
    public DateTime EndDate { get; set; }
    
    [Required]
    public int Days { get; set; }
    
    [Required]
    [MaxLength(1000)]
    public string Reason { get; set; } = string.Empty;
    
    [Required]
    public LeaveRequestStatus Status { get; set; } = LeaveRequestStatus.Pending;
    
    [MaxLength(36)]
    public string? ApprovedBy { get; set; }
    
    public DateTime SubmittedAt { get; set; } = DateTime.UtcNow;
    
    public DateTime? ReviewedAt { get; set; }
    
    [MaxLength(1000)]
    public string? Comments { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(EmployeeId))]
    public Employee Employee { get; set; } = null!;
    
    [ForeignKey(nameof(ApprovedBy))]
    public Employee? ApprovedByEmployee { get; set; }
}

// WorkSchedule.cs
[Table("WorkSchedules")]
[Index(nameof(EmployeeId), nameof(Date))]
[Index(nameof(Status))]
public class WorkSchedule : BaseEntity
{
    [Key]
    [MaxLength(36)]
    public string Id { get; set; } = Guid.NewGuid().ToString();
    
    [Required]
    [MaxLength(36)]
    public string EmployeeId { get; set; } = string.Empty;
    
    [Required]
    public DateTime Date { get; set; }
    
    [Required]
    [MaxLength(8)] // HH:MM format
    public string StartTime { get; set; } = string.Empty;
    
    [Required]
    [MaxLength(8)] // HH:MM format
    public string EndTime { get; set; } = string.Empty;
    
    [Required]
    public WorkScheduleType Type { get; set; } = WorkScheduleType.Regular;
    
    [Required]
    public WorkScheduleStatus Status { get; set; } = WorkScheduleStatus.Scheduled;
    
    [MaxLength(1000)]
    public string? Notes { get; set; }
    
    // Navigation properties
    [ForeignKey(nameof(EmployeeId))]
    public Employee Employee { get; set; } = null!;
}

3.5. ENUMS

// Core Enums
public enum UserRole
{
    Staff = 1,
    Manager = 2,
    Admin = 3
}

public enum OrderStatus
{
    Pending = 1,
    Processing = 2,
    Completed = 3,
    Cancelled = 4,
    Refunded = 5
}

public enum PaymentMethod
{
    Cash = 1,
    Card = 2,
    Digital = 3
}

public enum ShiftStatus
{
    Open = 1,
    Closed = 2
}

public enum TaskPriority
{
    Low = 1,
    Medium = 2,
    High = 3
}

public enum TaskStatus
{
    Pending = 1,
    InProgress = 2,
    Completed = 3
}

public enum InventoryMovementType
{
    Sale = 1,
    Purchase = 2,
    Adjustment = 3,
    Transfer = 4,
    Return = 5,
    Damage = 6
}

// CRM Enums
public enum CustomerStatus
{
    Lead = 1,
    Customer = 2,
    Inactive = 3
}

public enum CRMInteractionType
{
    Call = 1,
    Email = 2,
    Meeting = 3,
    Note = 4
}

public enum CRMInteractionStatus
{
    Pending = 1,
    Completed = 2
}

// LRM Enums
public enum SupplierStatus
{
    Active = 1,
    Inactive = 2
}

public enum StockTransferStatus
{
    Pending = 1,
    InTransit = 2,
    Delivered = 3,
    Cancelled = 4
}

public enum ShippingStatusType
{
    Pending = 1,
    Picked = 2,
    Shipped = 3,
    Delivered = 4,
    Cancelled = 5
}

// HRM Enums
public enum EmployeeStatus
{
    Active = 1,
    Inactive = 2,
    OnLeave = 3
}

public enum LeaveRequestType
{
    Vacation = 1,
    Sick = 2,
    Personal = 3,
    Emergency = 4
}

public enum LeaveRequestStatus
{
    Pending = 1,
    Approved = 2,
    Rejected = 3
}

public enum WorkScheduleType
{
    Regular = 1,
    Overtime = 2,
    Holiday = 3
}

public enum WorkScheduleStatus
{
    Scheduled = 1,
    Confirmed = 2,
    Completed = 3,
    Missed = 4
}

3.6. APPLICATION DBCONTEXT

// Data/ApplicationDbContext.cs
public class ApplicationDbContext : DbContext
{
    public ApplicationDbContext(DbContextOptions<ApplicationDbContext> options) : base(options)
    {
    }

    // Core Tables
    public DbSet<User> Users { get; set; }
    public DbSet<Store> Stores { get; set; }
    public DbSet<Product> Products { get; set; }
    public DbSet<InventoryItem> InventoryItems { get; set; }
    public DbSet<InventoryMovement> InventoryMovements { get; set; }
    public DbSet<Order> Orders { get; set; }
    public DbSet<OrderItem> OrderItems { get; set; }
    public DbSet<Shift> Shifts { get; set; }
    public DbSet<Task> Tasks { get; set; }
    public DbSet<Notification> Notifications { get; set; }

    // CRM Tables
    public DbSet<Customer> Customers { get; set; }
    public DbSet<CRMInteraction> CRMInteractions { get; set; }

    // LRM Tables
    public DbSet<Supplier> Suppliers { get; set; }
    public DbSet<StockTransfer> StockTransfers { get; set; }
    public DbSet<TransferItem> TransferItems { get; set; }
    public DbSet<ShippingStatus> ShippingStatuses { get; set; }

    // HRM Tables
    public DbSet<Employee> Employees { get; set; }
    public DbSet<PerformanceMetric> PerformanceMetrics { get; set; }
    public DbSet<LeaveRequest> LeaveRequests { get; set; }
    public DbSet<WorkSchedule> WorkSchedules { get; set; }

    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        base.OnModelCreating(modelBuilder);

        // Configure entity relationships and constraints
        ConfigureUserEntities(modelBuilder);
        ConfigureProductEntities(modelBuilder);
        ConfigureOrderEntities(modelBuilder);
        ConfigureInventoryEntities(modelBuilder);
        ConfigureShiftEntities(modelBuilder);
        ConfigureTaskEntities(modelBuilder);
        ConfigureNotificationEntities(modelBuilder);
        ConfigureCRMEntities(modelBuilder);
        ConfigureLRMEntities(modelBuilder);
        ConfigureHRMEntities(modelBuilder);
        
        // Configure many-to-many relationships
        ConfigureManyToManyRelationships(modelBuilder);
        
        // Apply global query filters
        ApplyGlobalQueryFilters(modelBuilder);
    }

    private void ConfigureUserEntities(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<User>(entity =>
        {
            // Self-referencing relationship for Manager
            entity.HasOne(u => u.Manager)
                  .WithMany(u => u.Subordinates)
                  .HasForeignKey(u => u.ManagerId)
                  .OnDelete(DeleteBehavior.Restrict);

            // Store relationship
            entity.HasOne(u => u.Store)
                  .WithMany(s => s.Users)
                  .HasForeignKey(u => u.StoreId)
                  .OnDelete(DeleteBehavior.Restrict);

            // Unique constraint on Email
            entity.HasIndex(u => u.Email).IsUnique();
        });

        modelBuilder.Entity<Store>(entity =>
        {
            entity.HasIndex(s => s.Name);
        });
    }

    private void ConfigureProductEntities(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<Product>(entity =>
        {
            entity.HasIndex(p => p.Barcode).IsUnique();
            entity.HasIndex(p => p.Category);
            entity.HasIndex(p => p.Name);
        });
    }

    private void ConfigureOrderEntities(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<Order>(entity =>
        {
            entity.HasIndex(o => o.OrderNumber).IsUnique();
            entity.HasIndex(o => new { o.StoreId, o.CreatedAt });

            // Staff relationship
            entity.HasOne(o => o.Staff)
                  .WithMany(u => u.Orders)
                  .HasForeignKey(o => o.StaffId)
                  .OnDelete(DeleteBehavior.Restrict);

            // Store relationship
            entity.HasOne(o => o.Store)
                  .WithMany(s => s.Orders)
                  .HasForeignKey(o => o.StoreId)
                  .OnDelete(DeleteBehavior.Restrict);

            // Customer relationship (optional)
            entity.HasOne(o => o.Customer)
                  .WithMany(c => c.Orders)
                  .HasForeignKey(o => o.CustomerId)
                  .OnDelete(DeleteBehavior.SetNull);
        });

        modelBuilder.Entity<OrderItem>(entity =>
        {
            entity.HasOne(oi => oi.Order)
                  .WithMany(o => o.Items)
                  .HasForeignKey(oi => oi.OrderId)
                  .OnDelete(DeleteBehavior.Cascade);

            entity.HasOne(oi => oi.Product)
                  .WithMany(p => p.OrderItems)
                  .HasForeignKey(oi => oi.ProductId)
                  .OnDelete(DeleteBehavior.Restrict);
        });
    }

    private void ConfigureInventoryEntities(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<InventoryItem>(entity =>
        {
            // Unique constraint for Product-Store combination
            entity.HasIndex(i => new { i.ProductId, i.StoreId }).IsUnique();

            entity.HasOne(i => i.Product)
                  .WithMany(p => p.Inventory)
                  .HasForeignKey(i => i.ProductId)
                  .OnDelete(DeleteBehavior.Cascade);

            entity.HasOne(i => i.Store)
                  .WithMany(s => s.Inventory)
                  .HasForeignKey(i => i.StoreId)
                  .OnDelete(DeleteBehavior.Cascade);
        });

        modelBuilder.Entity<InventoryMovement>(entity =>
        {
            entity.HasOne(im => im.InventoryItem)
                  .WithMany(i => i.Movements)
                  .HasForeignKey(im => im.InventoryItemId)
                  .OnDelete(DeleteBehavior.Cascade);

            entity.HasOne(im => im.Order)
                  .WithMany(o => o.InventoryMovements)
                  .HasForeignKey(im => im.OrderId)
                  .OnDelete(DeleteBehavior.SetNull);

            entity.HasOne(im => im.Transfer)
                  .WithMany(t => t.InventoryMovements)
                  .HasForeignKey(im => im.TransferId)
                  .OnDelete(DeleteBehavior.SetNull);
        });
    }

    private void ConfigureShiftEntities(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<Shift>(entity =>
        {
            entity.HasOne(s => s.Staff)
                  .WithMany(u => u.Shifts)
                  .HasForeignKey(s => s.StaffId)
                  .OnDelete(DeleteBehavior.Restrict);

            entity.HasOne(s => s.Store)
                  .WithMany(st => st.Shifts)
                  .HasForeignKey(s => s.StoreId)
                  .OnDelete(DeleteBehavior.Restrict);

            entity.HasIndex(s => new { s.StaffId, s.StartTime });
        });
    }

    private void ConfigureTaskEntities(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<Task>(entity =>
        {
            entity.HasOne(t => t.AssignedToUser)
                  .WithMany(u => u.AssignedTasks)
                  .HasForeignKey(t => t.AssignedTo)
                  .OnDelete(DeleteBehavior.Restrict);

            entity.HasOne(t => t.AssignedByUser)
                  .WithMany(u => u.CreatedTasks)
                  .HasForeignKey(t => t.AssignedBy)
                  .OnDelete(DeleteBehavior.Restrict);
        });
    }

    private void ConfigureNotificationEntities(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<Notification>(entity =>
        {
            entity.HasOne(n => n.User)
                  .WithMany(u => u.Notifications)
                  .HasForeignKey(n => n.UserId)
                  .OnDelete(DeleteBehavior.Cascade);

            entity.HasIndex(n => new { n.UserId, n.IsRead });
        });
    }

    private void ConfigureCRMEntities(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<Customer>(entity =>
        {
            entity.HasIndex(c => c.Email);
            entity.HasIndex(c => c.Phone);
        });

        modelBuilder.Entity<CRMInteraction>(entity =>
        {
            entity.HasOne(ci => ci.Customer)
                  .WithMany(c => c.Interactions)
                  .HasForeignKey(ci => ci.CustomerId)
                  .OnDelete(DeleteBehavior.Cascade);

            entity.HasOne(ci => ci.CreatedByUser)
                  .WithMany(u => u.CRMInteractions)
                  .HasForeignKey(ci => ci.CreatedBy)
                  .OnDelete(DeleteBehavior.Restrict);
        });
    }

    private void ConfigureLRMEntities(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<StockTransfer>(entity =>
        {
            entity.HasOne(st => st.RequestedByUser)
                  .WithMany()
                  .HasForeignKey(st => st.RequestedBy)
                  .OnDelete(DeleteBehavior.Restrict);

            entity.HasOne(st => st.ApprovedByUser)
                  .WithMany()
                  .HasForeignKey(st => st.ApprovedBy)
                  .OnDelete(DeleteBehavior.Restrict);

            entity.HasOne(st => st.Supplier)
                  .WithMany(s => s.StockTransfers)
                  .HasForeignKey(st => st.SupplierId)
                  .OnDelete(DeleteBehavior.SetNull);
        });

        modelBuilder.Entity<TransferItem>(entity =>
        {
            entity.HasOne(ti => ti.StockTransfer)
                  .WithMany(st => st.Items)
                  .HasForeignKey(ti => ti.StockTransferId)
                  .OnDelete(DeleteBehavior.Cascade);

            entity.HasOne(ti => ti.Product)
                  .WithMany(p => p.TransferItems)
                  .HasForeignKey(ti => ti.ProductId)
                  .OnDelete(DeleteBehavior.Restrict);
        });

        modelBuilder.Entity<ShippingStatus>(entity =>
        {
            entity.HasOne(ss => ss.Order)
                  .WithOne(o => o.ShippingStatus)
                  .HasForeignKey<ShippingStatus>(ss => ss.OrderId)
                  .OnDelete(DeleteBehavior.Cascade);

            entity.HasIndex(ss => ss.OrderId).IsUnique();
            entity.HasIndex(ss => ss.TrackingNumber);
        });
    }

    private void ConfigureHRMEntities(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<Employee>(entity =>
        {
            // Self-referencing relationship for Manager
            entity.HasOne(e => e.Manager)
                  .WithMany(e => e.Subordinates)
                  .HasForeignKey(e => e.ManagerId)
                  .OnDelete(DeleteBehavior.Restrict);

            // Store relationship
            entity.HasOne(e => e.Store)
                  .WithMany(s => s.Employees)
                  .HasForeignKey(e => e.StoreId)
                  .OnDelete(DeleteBehavior.SetNull);

            entity.HasIndex(e => e.Email).IsUnique();
        });

        modelBuilder.Entity<PerformanceMetric>(entity =>
        {
            entity.HasOne(pm => pm.Employee)
                  .WithMany(e => e.Performance)
                  .HasForeignKey(pm => pm.EmployeeId)
                  .OnDelete(DeleteBehavior.Cascade);

            entity.HasIndex(pm => new { pm.EmployeeId, pm.Period });
        });

        modelBuilder.Entity<LeaveRequest>(entity =>
        {
            entity.HasOne(lr => lr.Employee)
                  .WithMany(e => e.LeaveRequests)
                  .HasForeignKey(lr => lr.EmployeeId)
                  .OnDelete(DeleteBehavior.Cascade);

            entity.HasOne(lr => lr.ApprovedByEmployee)
                  .WithMany()
                  .HasForeignKey(lr => lr.ApprovedBy)
                  .OnDelete(DeleteBehavior.SetNull);
        });

        modelBuilder.Entity<WorkSchedule>(entity =>
        {
            entity.HasOne(ws => ws.Employee)
                  .WithMany(e => e.WorkSchedules)
                  .HasForeignKey(ws => ws.EmployeeId)
                  .OnDelete(DeleteBehavior.Cascade);

            entity.HasIndex(ws => new { ws.EmployeeId, ws.Date });
        });
    }

    private void ConfigureManyToManyRelationships(ModelBuilder modelBuilder)
    {
        // Product-Supplier many-to-many relationship
        modelBuilder.Entity<Product>()
            .HasMany(p => p.Suppliers)
            .WithMany(s => s.Products)
            .UsingEntity<Dictionary<string, object>>(
                "ProductSuppliers",
                j => j.HasOne<Supplier>().WithMany().HasForeignKey("SupplierId"),
                j => j.HasOne<Product>().WithMany().HasForeignKey("ProductId"),
                j =>
                {
                    j.HasKey("ProductId", "SupplierId");
                    j.ToTable("ProductSuppliers");
                });
    }

    private void ApplyGlobalQueryFilters(ModelBuilder modelBuilder)
    {
        // Global query filter for soft deletes
        foreach (var entityType in modelBuilder.Model.GetEntityTypes())
        {
            if (typeof(BaseEntity).IsAssignableFrom(entityType.ClrType))
            {
                modelBuilder.Entity(entityType.ClrType)
                    .HasQueryFilter(CreateGlobalQueryFilter(entityType.ClrType));
            }
        }
    }

    private LambdaExpression CreateGlobalQueryFilter(Type entityType)
    {
        var parameter = Expression.Parameter(entityType, "e");
        var isDeletedProperty = Expression.Property(parameter, nameof(BaseEntity.IsDeleted));
        var body = Expression.Equal(isDeletedProperty, Expression.Constant(false));
        return Expression.Lambda(body, parameter);
    }

    public override Task<int> SaveChangesAsync(CancellationToken cancellationToken = default)
    {
        foreach (var entry in ChangeTracker.Entries<BaseEntity>())
        {
            switch (entry.State)
            {
                case EntityState.Added:
                    entry.Entity.CreatedAt = DateTime.UtcNow;
                    entry.Entity.UpdatedAt = DateTime.UtcNow;
                    break;
                case EntityState.Modified:
                    entry.Entity.UpdatedAt = DateTime.UtcNow;
                    break;
                case EntityState.Deleted:
                    entry.State = EntityState.Modified;
                    entry.Entity.IsDeleted = true;
                    entry.Entity.UpdatedAt = DateTime.UtcNow;
                    break;
            }
        }

        return base.SaveChangesAsync(cancellationToken);
    }
}

3.7. ENTITY CONFIGURATIONS

// Configurations/UserConfiguration.cs
public class UserConfiguration : IEntityTypeConfiguration<User>
{
    public void Configure(EntityTypeBuilder<User> builder)
    {
        builder.ToTable("Users");
        
        builder.HasKey(u => u.Id);
        
        builder.Property(u => u.Id)
            .HasMaxLength(36)
            .IsRequired();
            
        builder.Property(u => u.Email)
            .HasMaxLength(255)
            .IsRequired();
            
        builder.Property(u => u.Name)
            .HasMaxLength(100)
            .IsRequired();
            
        builder.Property(u => u.PasswordHash)
            .HasMaxLength(255)
            .IsRequired();
            
        builder.Property(u => u.Role)
            .HasConversion<int>()
            .IsRequired();
            
        builder.Property(u => u.StoreId)
            .HasMaxLength(36)
            .IsRequired();
            
        builder.Property(u => u.ManagerId)
            .HasMaxLength(36);
            
        builder.Property(u => u.Avatar)
            .HasMaxLength(500);
            
        builder.Property(u => u.IsActive)
            .HasDefaultValue(true);
            
        // Indexes
        builder.HasIndex(u => u.Email).IsUnique();
        builder.HasIndex(u => u.StoreId);
        builder.HasIndex(u => u.ManagerId);
        
        // Relationships
        builder.HasOne(u => u.Store)
            .WithMany(s => s.Users)
            .HasForeignKey(u => u.StoreId)
            .OnDelete(DeleteBehavior.Restrict);
            
        builder.HasOne(u => u.Manager)
            .WithMany(u => u.Subordinates)
            .HasForeignKey(u => u.ManagerId)
            .OnDelete(DeleteBehavior.Restrict);
    }
}

// Configurations/ProductConfiguration.cs
public class ProductConfiguration : IEntityTypeConfiguration<Product>
{
    public void Configure(EntityTypeBuilder<Product> builder)
    {
        builder.ToTable("Products");
        
        builder.HasKey(p => p.Id);
        
        builder.Property(p => p.Id)
            .HasMaxLength(36)
            .IsRequired();
            
        builder.Property(p => p.Name)
            .HasMaxLength(200)
            .IsRequired();
            
        builder.Property(p => p.Description)
            .HasMaxLength(1000);
            
        builder.Property(p => p.Barcode)
            .HasMaxLength(50)
            .IsRequired();
            
        builder.Property(p => p.Price)
            .HasPrecision(10, 2)
            .IsRequired();
            
        builder.Property(p => p.Cost)
            .HasPrecision(10, 2)
            .IsRequired();
            
        builder.Property(p => p.Category)
            .HasMaxLength(100)
            .IsRequired();
            
        builder.Property(p => p.Image)
            .HasMaxLength(500);
            
        builder.Property(p => p.IsActive)
            .HasDefaultValue(true);
            
        // Indexes
        builder.HasIndex(p => p.Barcode).IsUnique();
        builder.HasIndex(p => p.Category);
        builder.HasIndex(p => p.Name);
        builder.HasIndex(p => p.IsActive);
    }
}

// Configurations/OrderConfiguration.cs
public class OrderConfiguration : IEntityTypeConfiguration<Order>
{
    public void Configure(EntityTypeBuilder<Order> builder)
    {
        builder.ToTable("Orders");
        
        builder.HasKey(o => o.Id);
        
        builder.Property(o => o.Id)
            .HasMaxLength(36)
            .IsRequired();
            
        builder.Property(o => o.OrderNumber)
            .HasMaxLength(50)
            .IsRequired();
            
        builder.Property(o => o.CustomerId)
            .HasMaxLength(36);
            
        builder.Property(o => o.CustomerName)
            .HasMaxLength(100);
            
        builder.Property(o => o.CustomerPhone)
            .HasMaxLength(20);
            
        builder.Property(o => o.StoreId)
            .HasMaxLength(36)
            .IsRequired();
            
        builder.Property(o => o.StaffId)
            .HasMaxLength(36)
            .IsRequired();
            
        builder.Property(o => o.Status)
            .HasConversion<int>()
            .IsRequired();
            
        builder.Property(o => o.Subtotal)
            .HasPrecision(10, 2)
            .IsRequired();
            
        builder.Property(o => o.Tax)
            .HasPrecision(10, 2)
            .IsRequired();
            
        builder.Property(o => o.Discount)
            .HasPrecision(10, 2)
            .IsRequired();
            
        builder.Property(o => o.Tip)
            .HasPrecision(10, 2)
            .IsRequired();
            
        builder.Property(o => o.Total)
            .HasPrecision(10, 2)
            .IsRequired();
            
        builder.Property(o => o.PaymentMethod)
            .HasConversion<int>()
            .IsRequired();
            
        builder.Property(o => o.PaymentStatus)
            .HasMaxLength(20)
            .IsRequired();
            
        builder.Property(o => o.Notes)
            .HasMaxLength(1000);
            
        // Indexes
        builder.HasIndex(o => o.OrderNumber).IsUnique();
        builder.HasIndex(o => new { o.StoreId, o.CreatedAt });
        builder.HasIndex(o => o.Status);
        builder.HasIndex(o => o.PaymentStatus);
        builder.HasIndex(o => o.CustomerId);
        
        // Relationships
        builder.HasOne(o => o.Store)
            .WithMany(s => s.Orders)
            .HasForeignKey(o => o.StoreId)
            .OnDelete(DeleteBehavior.Restrict);
            
        builder.HasOne(o => o.Staff)
            .WithMany(u => u.Orders)
            .HasForeignKey(o => o.StaffId)
            .OnDelete(DeleteBehavior.Restrict);
            
        builder.HasOne(o => o.Customer)
            .WithMany(c => c.Orders)
            .HasForeignKey(o => o.CustomerId)
            .OnDelete(DeleteBehavior.SetNull);
    }
}

// Configurations/InventoryItemConfiguration.cs
public class InventoryItemConfiguration : IEntityTypeConfiguration<InventoryItem>
{
    public void Configure(EntityTypeBuilder<InventoryItem> builder)
    {
        builder.ToTable("InventoryItems");
        
        builder.HasKey(i => i.Id);
        
        builder.Property(i => i.Id)
            .HasMaxLength(36)
            .IsRequired();
            
        builder.Property(i => i.ProductId)
            .HasMaxLength(36)
            .IsRequired();
            
        builder.Property(i => i.StoreId)
            .HasMaxLength(36)
            .IsRequired();
            
        builder.Property(i => i.Quantity)
            .IsRequired();
            
        builder.Property(i => i.MinStock)
            .IsRequired();
            
        builder.Property(i => i.MaxStock)
            .IsRequired();
            
        builder.Property(i => i.LastUpdated)
            .IsRequired();
            
        // Indexes
        builder.HasIndex(i => new { i.ProductId, i.StoreId }).IsUnique();
        builder.HasIndex(i => i.Quantity);
        
        // Relationships
        builder.HasOne(i => i.Product)
            .WithMany(p => p.Inventory)
            .HasForeignKey(i => i.ProductId)
            .OnDelete(DeleteBehavior.Cascade);
            
        builder.HasOne(i => i.Store)
            .WithMany(s => s.Inventory)
            .HasForeignKey(i => i.StoreId)
            .OnDelete(DeleteBehavior.Cascade);
    }
}

3.8. SEEDER CLASSES

// Seeds/DataSeeder.cs
public class DataSeeder
{
    private readonly ApplicationDbContext _context;
    
    public DataSeeder(ApplicationDbContext context)
    {
        _context = context;
    }
    
    public async Task SeedAsync()
    {
        await SeedStoresAsync();
        await SeedUsersAsync();
        await SeedProductsAsync();
        await SeedInventoryAsync();
        await SeedCustomersAsync();
        await SeedSuppliersAsync();
        await SeedEmployeesAsync();
        
        await _context.SaveChangesAsync();
    }
    
    private async Task SeedStoresAsync()
    {
        if (!await _context.Stores.AnyAsync())
        {
            var stores = new[]
            {
                new Store
                {
                    Id = "store-1",
                    Name = "Main Store",
                    Address = "123 Main St, City, Country",
                    Phone = "+1234567890",
                    Email = "main@store.com",
                    Currency = "USD",
                    Timezone = "America/New_York",
                    IsActive = true,
                    CreatedBy = "system"
                },
                new Store
                {
                    Id = "store-2",
                    Name = "Branch Store",
                    Address = "456 Branch Ave, City, Country",
                    Phone = "+1234567891",
                    Email = "branch@store.com",
                    Currency = "USD",
                    Timezone = "America/New_York",
                    IsActive = true,
                    CreatedBy = "system"
                }
            };
            
            await _context.Stores.AddRangeAsync(stores);
        }
    }
    
    private async Task SeedUsersAsync()
    {
        if (!await _context.Users.AnyAsync())
        {
            var passwordHash = BCrypt.Net.BCrypt.HashPassword("password");
            
            var users = new[]
            {
                new User
                {
                    Id = "user-1",
                    Email = "staff@store.com",
                    Name = "John Staff",
                    PasswordHash = passwordHash,
                    Role = UserRole.Staff,
                    StoreId = "store-1",
                    ManagerId = "user-2",
                    IsActive = true,
                    CreatedBy = "system"
                },
                new User
                {
                    Id = "user-2",
                    Email = "manager@store.com",
                    Name = "Jane Manager",
                    PasswordHash = passwordHash,
                    Role = UserRole.Manager,
                    StoreId = "store-1",
                    IsActive = true,
                    CreatedBy = "system"
                },
                new User
                {
                    Id = "user-3",
                    Email = "admin@store.com",
                    Name = "Admin User",
                    PasswordHash = passwordHash,
                    Role = UserRole.Admin,
                    StoreId = "store-1",
                    IsActive = true,
                    CreatedBy = "system"
                }
            };
            
            await _context.Users.AddRangeAsync(users);
        }
    }
    
    private async Task SeedProductsAsync()
    {
        if (!await _context.Products.AnyAsync())
        {
            var products = new[]
            {
                new Product
                {
                    Id = "product-1",
                    Name = "Coffee Mug",
                    Description = "Premium ceramic coffee mug",
                    Barcode = "1234567890123",
                    Price = 12.99m,
                    Cost = 6.50m,
                    Category = "Drinkware",
                    IsActive = true,
                    CreatedBy = "system"
                },
                new Product
                {
                    Id = "product-2",
                    Name = "T-Shirt",
                    Description = "Comfortable cotton t-shirt",
                    Barcode = "2345678901234",
                    Price = 19.99m,
                    Cost = 8.00m,
                    Category = "Clothing",
                    IsActive = true,
                    CreatedBy = "system"
                },
                new Product
                {
                    Id = "product-3",
                    Name = "Notebook",
                    Description = "Spiral-bound notebook",
                    Barcode = "3456789012345",
                    Price = 8.99m,
                    Cost = 3.50m,
                    Category = "Stationery",
                    IsActive = true,
                    CreatedBy = "system"
                }
            };
            
            await _context.Products.AddRangeAsync(products);
        }
    }
    
    private async Task SeedInventoryAsync()
    {
        if (!await _context.InventoryItems.AnyAsync())
        {
            var inventory = new[]
            {
                new InventoryItem
                {
                    Id = "inv-1",
                    ProductId = "product-1",
                    StoreId = "store-1",
                    Quantity = 25,
                    MinStock = 10,
                    MaxStock = 100,
                    LastUpdated = DateTime.UtcNow,
                    CreatedBy = "system"
                },
                new InventoryItem
                {
                    Id = "inv-2",
                    ProductId = "product-2",
                    StoreId = "store-1",
                    Quantity = 5,
                    MinStock = 10,
                    MaxStock = 50,
                    LastUpdated = DateTime.UtcNow,
                    CreatedBy = "system"
                },
                new InventoryItem
                {
                    Id = "inv-3",
                    ProductId = "product-3",
                    StoreId = "store-1",
                    Quantity = 45,
                    MinStock = 20,
                    MaxStock = 100,
                    LastUpdated = DateTime.UtcNow,
                    CreatedBy = "system"
                }
            };
            
            await _context.InventoryItems.AddRangeAsync(inventory);
        }
    }
    
    private async Task SeedCustomersAsync()
    {
        if (!await _context.Customers.AnyAsync())
        {
            var customers = new[]
            {
                new Customer
                {
                    Id = "customer-1",
                    Name = "Alice Johnson",
                    Email = "alice.johnson@email.com",
                    Phone = "+1234567890",
                    Address = "123 Oak Street, City, State 12345",
                    Company = "Tech Solutions Inc.",
                    Status = CustomerStatus.Customer,
                    TotalOrders = 15,
                    TotalSpent = 2450.75m,
                    LastOrderDate = DateTime.UtcNow.AddDays(-5),
                    CreatedBy = "system"
                },
                new Customer
                {
                    Id = "customer-2",
                    Name = "Bob Smith",
                    Email = "bob.smith@email.com",
                    Phone = "+1234567891",
                    Address = "456 Pine Avenue, City, State 12345",
                    Status = CustomerStatus.Lead,
                    TotalOrders = 0,
                    TotalSpent = 0,
                    CreatedBy = "system"
                }
            };
            
            await _context.Customers.AddRangeAsync(customers);
        }
    }
    
    private async Task SeedSuppliersAsync()
    {
        if (!await _context.Suppliers.AnyAsync())
        {
            var suppliers = new[]
            {
                new Supplier
                {
                    Id = "supplier-1",
                    Name = "MugCorp Industries",
                    Email = "orders@mugcorp.com",
                    Phone = "+1555123456",
                    Address = "789 Industrial Blvd, Factory City, State 54321",
                    Status = SupplierStatus.Active,
                    PaymentTerms = "Net 30",
                    DeliverySchedule = "Weekly",
                    CreatedBy = "system"
                }
            };
            
            await _context.Suppliers.AddRangeAsync(suppliers);
        }
    }
    
    private async Task SeedEmployeesAsync()
    {
        if (!await _context.Employees.AnyAsync())
        {
            var employees = new[]
            {
                new Employee
                {
                    Id = "emp-1",
                    Name = "John Staff",
                    Email = "john.staff@store.com",
                    Phone = "+1234567890",
                    Position = "Sales Associate",
                    Department = "Sales",
                    HireDate = DateTime.UtcNow.AddMonths(-6),
                    Salary = 35000,
                    Status = EmployeeStatus.Active,
                    StoreId = "store-1",
                    Skills = "[\"Customer Service\", \"POS Systems\", \"Inventory Management\"]",
                    CreatedBy = "system"
                }
            };
            
            await _context.Employees.AddRangeAsync(employees);
        }
    }
}

================================================================================
4. AUTHENTICATION & AUTHORIZATION
================================================================================

4.1. JWT AUTHENTICATION SERVICE

// JwtTokenService.cs
public class JwtTokenService
{
    private readonly IConfiguration _configuration;
    
    public JwtTokenService(IConfiguration configuration)
    {
        _configuration = configuration;
    }
    
    public string GenerateToken(User user, IEnumerable<string> permissions)
    {
        var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_configuration["Jwt:Key"]));
        var credentials = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);
        
        var claims = new List<Claim>
        {
            new Claim(ClaimTypes.NameIdentifier, user.Id),
            new Claim(ClaimTypes.Name, user.Name),
            new Claim(ClaimTypes.Email, user.Email),
            new Claim("role", user.Role.ToString()),
            new Claim("storeId", user.StoreId),
        };
        
        // Add permissions as claims
        foreach (var permission in permissions)
        {
            claims.Add(new Claim("permission", permission));
        }
        
        var token = new JwtSecurityToken(
            issuer: _configuration["Jwt:Issuer"],
            audience: _configuration["Jwt:Audience"],
            claims: claims,
            expires: DateTime.Now.AddHours(24),
            signingCredentials: credentials
        );
        
        return new JwtSecurityTokenHandler().WriteToken(token);
    }
    
    public string GenerateRefreshToken()
    {
        var randomNumber = new byte[32];
        using var rng = RandomNumberGenerator.Create();
        rng.GetBytes(randomNumber);
        return Convert.ToBase64String(randomNumber);
    }
}

4.2. PERMISSION CONSTANTS

// Permissions.cs
public static class Permissions
{
    // Orders
    public const string ViewOwnOrders = "orders.view_own";
    public const string ViewAllOrders = "orders.view_all";
    public const string CreateOrder = "orders.create";
    public const string UpdateOrder = "orders.update";
    public const string DeleteOrder = "orders.delete";
    public const string AssignOrder = "orders.assign";
    
    // POS
    public const string AccessPOS = "pos.access";
    public const string ScanBarcode = "pos.scan_barcode";
    public const string ProcessPayment = "pos.process_payment";
    public const string OpenCloseShift = "pos.open_close_shift";
    
    // Inventory
    public const string ViewInventory = "inventory.view";
    public const string ManageInventory = "inventory.manage";
    public const string TransferStock = "inventory.transfer";
    public const string AdjustStock = "inventory.adjust";
    
    // Staff Management
    public const string ViewStaff = "users.view_all";
    public const string ManageStaff = "users.manage";
    public const string AssignTasks = "tasks.assign";
    public const string ViewPerformance = "analytics.view_staff_reports";
    
    // Tasks
    public const string ViewOwnTasks = "tasks.view_own";
    public const string ViewAllTasks = "tasks.view_all";
    public const string CreateTasks = "tasks.create";
    
    // Analytics & Reports
    public const string ViewAnalytics = "analytics.view_store_dashboard";
    public const string ExportReports = "analytics.export_reports";
    public const string ScheduleReports = "analytics.schedule_reports";
    
    // CRM
    public const string ViewCRM = "crm.view";
    public const string EditCRM = "crm.edit";
    public const string CreateCRMInteractions = "crm.create_interactions";
    public const string ViewCRMAnalytics = "crm.view_analytics";
    
    // LRM
    public const string ViewLRM = "lrm.view";
    public const string EditLRM = "lrm.edit";
    public const string ManageSuppliers = "lrm.manage_suppliers";
    public const string RequestTransfers = "lrm.request_transfers";
    public const string ConfirmDeliveries = "lrm.confirm_deliveries";
    
    // HRM
    public const string ViewOwnHRM = "hrm.view_own";
    public const string ViewAllHRM = "hrm.view_all";
    public const string ManageHRM = "hrm.manage";
    public const string SubmitLeaveRequests = "hrm.submit_leave_requests";
    public const string ApproveLeaveRequests = "hrm.approve_leave_requests";
    public const string AssignShifts = "hrm.assign_shifts";
    
    // System
    public const string ManageSettings = "system.manage_settings";
    public const string ViewAuditLog = "system.view_audit_log";
    public const string SendBroadcast = "system.send_broadcast";
}

4.3. PERMISSION ATTRIBUTE

// PermissionAttribute.cs
public class RequirePermissionAttribute : Attribute, IAuthorizationFilter
{
    private readonly string _permission;
    
    public RequirePermissionAttribute(string permission)
    {
        _permission = permission;
    }
    
    public void OnAuthorization(AuthorizationFilterContext context)
    {
        var user = context.HttpContext.User;
        
        if (!user.Identity.IsAuthenticated)
        {
            context.Result = new UnauthorizedResult();
            return;
        }
        
        var permissions = user.Claims
            .Where(c => c.Type == "permission")
            .Select(c => c.Value);
            
        if (!permissions.Contains(_permission))
        {
            context.Result = new ForbidResult();
            return;
        }
    }
}

================================================================================
5. API CONTROLLERS & ENDPOINTS
================================================================================

5.1. AUTH CONTROLLER

// AuthController.cs
[ApiController]
[Route("api/[controller]")]
public class AuthController : ControllerBase
{
    private readonly IAuthService _authService;
    
    public AuthController(IAuthService authService)
    {
        _authService = authService;
    }
    
    [HttpPost("login")]
    public async Task<ActionResult<LoginResponseDto>> Login([FromBody] LoginRequestDto request)
    {
        var result = await _authService.LoginAsync(request.Email, request.Password);
        
        if (!result.Success)
        {
            return BadRequest(new { message = result.Message });
        }
        
        return Ok(result.Data);
    }
    
    [HttpPost("refresh")]
    public async Task<ActionResult<LoginResponseDto>> RefreshToken([FromBody] RefreshTokenRequestDto request)
    {
        var result = await _authService.RefreshTokenAsync(request.RefreshToken);
        
        if (!result.Success)
        {
            return BadRequest(new { message = result.Message });
        }
        
        return Ok(result.Data);
    }
    
    [HttpPost("logout")]
    [Authorize]
    public async Task<IActionResult> Logout()
    {
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        await _authService.LogoutAsync(userId);
        return Ok(new { message = "Logged out successfully" });
    }
    
    [HttpGet("me")]
    [Authorize]
    public async Task<ActionResult<UserDto>> GetCurrentUser()
    {
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        var user = await _authService.GetCurrentUserAsync(userId);
        return Ok(user);
    }
}

5.2. PRODUCTS CONTROLLER

// ProductsController.cs
[ApiController]
[Route("api/[controller]")]
[Authorize]
public class ProductsController : ControllerBase
{
    private readonly IProductService _productService;
    
    public ProductsController(IProductService productService)
    {
        _productService = productService;
    }
    
    [HttpGet]
    [RequirePermission(Permissions.ViewInventory)]
    public async Task<ActionResult<PagedResultDto<ProductDto>>> GetProducts(
        [FromQuery] int page = 1,
        [FromQuery] int pageSize = 10,
        [FromQuery] string? search = null,
        [FromQuery] string? category = null,
        [FromQuery] bool? isActive = null)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var result = await _productService.GetProductsAsync(storeId, page, pageSize, search, category, isActive);
        return Ok(result);
    }
    
    [HttpGet("{id}")]
    [RequirePermission(Permissions.ViewInventory)]
    public async Task<ActionResult<ProductDto>> GetProduct(string id)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var product = await _productService.GetProductByIdAsync(id, storeId);
        
        if (product == null)
        {
            return NotFound();
        }
        
        return Ok(product);
    }
    
    [HttpPost]
    [RequirePermission(Permissions.ManageInventory)]
    public async Task<ActionResult<ProductDto>> CreateProduct([FromBody] CreateProductDto request)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        var product = await _productService.CreateProductAsync(request, storeId, userId);
        return CreatedAtAction(nameof(GetProduct), new { id = product.Id }, product);
    }
    
    [HttpPut("{id}")]
    [RequirePermission(Permissions.ManageInventory)]
    public async Task<ActionResult<ProductDto>> UpdateProduct(string id, [FromBody] UpdateProductDto request)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        var product = await _productService.UpdateProductAsync(id, request, storeId, userId);
        
        if (product == null)
        {
            return NotFound();
        }
        
        return Ok(product);
    }
    
    [HttpDelete("{id}")]
    [RequirePermission(Permissions.ManageInventory)]
    public async Task<IActionResult> DeleteProduct(string id)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        var success = await _productService.DeleteProductAsync(id, storeId, userId);
        
        if (!success)
        {
            return NotFound();
        }
        
        return NoContent();
    }
    
    [HttpGet("barcode/{barcode}")]
    [RequirePermission(Permissions.AccessPOS)]
    public async Task<ActionResult<ProductDto>> GetProductByBarcode(string barcode)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var product = await _productService.GetProductByBarcodeAsync(barcode, storeId);
        
        if (product == null)
        {
            return NotFound();
        }
        
        return Ok(product);
    }
}

5.3. ORDERS CONTROLLER

// OrdersController.cs
[ApiController]
[Route("api/[controller]")]
[Authorize]
public class OrdersController : ControllerBase
{
    private readonly IOrderService _orderService;
    private readonly IHubContext<NotificationHub> _hubContext;
    
    public OrdersController(IOrderService orderService, IHubContext<NotificationHub> hubContext)
    {
        _orderService = orderService;
        _hubContext = hubContext;
    }
    
    [HttpGet]
    public async Task<ActionResult<PagedResultDto<OrderDto>>> GetOrders(
        [FromQuery] int page = 1,
        [FromQuery] int pageSize = 10,
        [FromQuery] string? search = null,
        [FromQuery] OrderStatus? status = null,
        [FromQuery] DateTime? startDate = null,
        [FromQuery] DateTime? endDate = null)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        var canViewAllOrders = User.HasClaim("permission", Permissions.ViewAllOrders);
        
        var staffId = canViewAllOrders ? null : userId;
        
        var result = await _orderService.GetOrdersAsync(
            storeId, staffId, page, pageSize, search, status, startDate, endDate);
            
        return Ok(result);
    }
    
    [HttpGet("{id}")]
    public async Task<ActionResult<OrderDto>> GetOrder(string id)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        var canViewAllOrders = User.HasClaim("permission", Permissions.ViewAllOrders);
        
        var order = await _orderService.GetOrderByIdAsync(id, storeId);
        
        if (order == null)
        {
            return NotFound();
        }
        
        // Check if user can view this order
        if (!canViewAllOrders && order.StaffId != userId)
        {
            return Forbid();
        }
        
        return Ok(order);
    }
    
    [HttpPost]
    [RequirePermission(Permissions.CreateOrder)]
    public async Task<ActionResult<OrderDto>> CreateOrder([FromBody] CreateOrderDto request)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        var order = await _orderService.CreateOrderAsync(request, storeId, userId);
        
        // Send real-time notification
        await _hubContext.Clients.Group($"store_{storeId}")
            .SendAsync("OrderCreated", new { OrderId = order.Id, OrderNumber = order.OrderNumber });
        
        return CreatedAtAction(nameof(GetOrder), new { id = order.Id }, order);
    }
    
    [HttpPut("{id}")]
    [RequirePermission(Permissions.UpdateOrder)]
    public async Task<ActionResult<OrderDto>> UpdateOrder(string id, [FromBody] UpdateOrderDto request)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        var order = await _orderService.UpdateOrderAsync(id, request, storeId, userId);
        
        if (order == null)
        {
            return NotFound();
        }
        
        // Send real-time notification
        await _hubContext.Clients.Group($"store_{storeId}")
            .SendAsync("OrderUpdated", new { OrderId = order.Id, Status = order.Status });
        
        return Ok(order);
    }
    
    [HttpPost("{id}/complete")]
    [RequirePermission(Permissions.UpdateOrder)]
    public async Task<IActionResult> CompleteOrder(string id)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        var success = await _orderService.CompleteOrderAsync(id, storeId, userId);
        
        if (!success)
        {
            return NotFound();
        }
        
        // Send real-time notification
        await _hubContext.Clients.Group($"store_{storeId}")
            .SendAsync("OrderCompleted", new { OrderId = id });
        
        return Ok(new { message = "Order completed successfully" });
    }
    
    [HttpPost("{id}/cancel")]
    [RequirePermission(Permissions.UpdateOrder)]
    public async Task<IActionResult> CancelOrder(string id, [FromBody] CancelOrderDto request)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        var success = await _orderService.CancelOrderAsync(id, request.Reason, storeId, userId);
        
        if (!success)
        {
            return NotFound();
        }
        
        return Ok(new { message = "Order cancelled successfully" });
    }
}

5.4. INVENTORY CONTROLLER

// InventoryController.cs
[ApiController]
[Route("api/[controller]")]
[Authorize]
[RequirePermission(Permissions.ViewInventory)]
public class InventoryController : ControllerBase
{
    private readonly IInventoryService _inventoryService;
    
    public InventoryController(IInventoryService inventoryService)
    {
        _inventoryService = inventoryService;
    }
    
    [HttpGet]
    public async Task<ActionResult<PagedResultDto<InventoryItemDto>>> GetInventory(
        [FromQuery] int page = 1,
        [FromQuery] int pageSize = 10,
        [FromQuery] string? search = null,
        [FromQuery] bool? lowStock = null)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var result = await _inventoryService.GetInventoryAsync(storeId, page, pageSize, search, lowStock);
        return Ok(result);
    }
    
    [HttpPost("adjust")]
    [RequirePermission(Permissions.AdjustStock)]
    public async Task<IActionResult> AdjustStock([FromBody] StockAdjustmentDto request)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        await _inventoryService.AdjustStockAsync(request, storeId, userId);
        
        return Ok(new { message = "Stock adjusted successfully" });
    }
    
    [HttpGet("low-stock")]
    public async Task<ActionResult<List<InventoryItemDto>>> GetLowStockItems()
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var result = await _inventoryService.GetLowStockItemsAsync(storeId);
        return Ok(result);
    }
    
    [HttpPost("receive")]
    [RequirePermission(Permissions.ManageInventory)]
    public async Task<IActionResult> ReceiveInventory([FromBody] ReceiveInventoryDto request)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        await _inventoryService.ReceiveInventoryAsync(request, storeId, userId);
        
        return Ok(new { message = "Inventory received successfully" });
    }
}

5.5. ANALYTICS CONTROLLER

// AnalyticsController.cs
[ApiController]
[Route("api/[controller]")]
[Authorize]
[RequirePermission(Permissions.ViewAnalytics)]
public class AnalyticsController : ControllerBase
{
    private readonly IAnalyticsService _analyticsService;
    
    public AnalyticsController(IAnalyticsService analyticsService)
    {
        _analyticsService = analyticsService;
    }
    
    [HttpGet("dashboard")]
    public async Task<ActionResult<AnalyticsDto>> GetDashboardAnalytics(
        [FromQuery] DateTime? startDate = null,
        [FromQuery] DateTime? endDate = null)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var result = await _analyticsService.GetDashboardAnalyticsAsync(storeId, startDate, endDate);
        return Ok(result);
    }
    
    [HttpGet("revenue")]
    public async Task<ActionResult<RevenueReportDto>> GetRevenueReport(
        [FromQuery] DateTime? startDate = null,
        [FromQuery] DateTime? endDate = null,
        [FromQuery] string groupBy = "day")
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var result = await _analyticsService.GetRevenueReportAsync(storeId, startDate, endDate, groupBy);
        return Ok(result);
    }
    
    [HttpGet("sales")]
    public async Task<ActionResult<SalesReportDto>> GetSalesReport(
        [FromQuery] DateTime? startDate = null,
        [FromQuery] DateTime? endDate = null)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var result = await _analyticsService.GetSalesReportAsync(storeId, startDate, endDate);
        return Ok(result);
    }
    
    [HttpGet("staff-performance")]
    [RequirePermission(Permissions.ViewPerformance)]
    public async Task<ActionResult<List<PerformanceReportDto>>> GetStaffPerformance(
        [FromQuery] DateTime? startDate = null,
        [FromQuery] DateTime? endDate = null)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var result = await _analyticsService.GetStaffPerformanceAsync(storeId, startDate, endDate);
        return Ok(result);
    }
    
    [HttpPost("export")]
    [RequirePermission(Permissions.ExportReports)]
    public async Task<IActionResult> ExportReport([FromBody] ExportRequestDto request)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var fileBytes = await _analyticsService.ExportReportAsync(request, storeId);
        
        return File(fileBytes, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", 
                   $"report_{DateTime.Now:yyyyMMdd}.xlsx");
    }
}

================================================================================
6. DTOs VÀ MAPPING
================================================================================

6.1. COMMON DTOs

// PagedResultDto.cs
public class PagedResultDto<T>
{
    public List<T> Items { get; set; } = new List<T>();
    public int TotalCount { get; set; }
    public int Page { get; set; }
    public int PageSize { get; set; }
    public int TotalPages => (int)Math.Ceiling((double)TotalCount / PageSize);
    public bool HasNextPage => Page < TotalPages;
    public bool HasPreviousPage => Page > 1;
}

// ApiResponseDto.cs
public class ApiResponseDto<T>
{
    public bool Success { get; set; }
    public string Message { get; set; }
    public T? Data { get; set; }
    public List<string> Errors { get; set; } = new List<string>();
    
    public static ApiResponseDto<T> SuccessResult(T data, string message = "Success")
    {
        return new ApiResponseDto<T>
        {
            Success = true,
            Message = message,
            Data = data
        };
    }
    
    public static ApiResponseDto<T> ErrorResult(string message, List<string> errors = null)
    {
        return new ApiResponseDto<T>
        {
            Success = false,
            Message = message,
            Errors = errors ?? new List<string>()
        };
    }
}

6.2. AUTH DTOs

// LoginRequestDto.cs
public class LoginRequestDto
{
    [Required]
    [EmailAddress]
    public string Email { get; set; }
    
    [Required]
    [MinLength(6)]
    public string Password { get; set; }
}

// LoginResponseDto.cs
public class LoginResponseDto
{
    public string AccessToken { get; set; }
    public string RefreshToken { get; set; }
    public DateTime ExpiresAt { get; set; }
    public UserDto User { get; set; }
    public StoreDto CurrentStore { get; set; }
    public List<StoreDto> AvailableStores { get; set; } = new List<StoreDto>();
}

// UserDto.cs
public class UserDto
{
    public string Id { get; set; }
    public string Email { get; set; }
    public string Name { get; set; }
    public string Role { get; set; }
    public string StoreId { get; set; }
    public string? ManagerId { get; set; }
    public string? Avatar { get; set; }
    public List<string> Permissions { get; set; } = new List<string>();
    public bool IsActive { get; set; }
    public DateTime CreatedAt { get; set; }
    public DateTime? LastLogin { get; set; }
}

6.3. PRODUCT DTOs

// ProductDto.cs
public class ProductDto
{
    public string Id { get; set; }
    public string Name { get; set; }
    public string Description { get; set; }
    public string Barcode { get; set; }
    public decimal Price { get; set; }
    public decimal Cost { get; set; }
    public string Category { get; set; }
    public string? Image { get; set; }
    public bool IsActive { get; set; }
    public DateTime CreatedAt { get; set; }
    public DateTime UpdatedAt { get; set; }
}

// CreateProductDto.cs
public class CreateProductDto
{
    [Required]
    [MaxLength(200)]
    public string Name { get; set; }
    
    [MaxLength(1000)]
    public string Description { get; set; }
    
    [Required]
    [MaxLength(50)]
    public string Barcode { get; set; }
    
    [Required]
    [Range(0.01, 999999.99)]
    public decimal Price { get; set; }
    
    [Required]
    [Range(0, 999999.99)]
    public decimal Cost { get; set; }
    
    [Required]
    [MaxLength(100)]
    public string Category { get; set; }
    
    public string? Image { get; set; }
    
    // Initial inventory settings
    [Required]
    [Range(0, int.MaxValue)]
    public int InitialQuantity { get; set; }
    
    [Required]
    [Range(1, int.MaxValue)]
    public int MinStock { get; set; }
    
    [Required]
    [Range(1, int.MaxValue)]
    public int MaxStock { get; set; }
}

6.4. ORDER DTOs

// OrderDto.cs
public class OrderDto
{
    public string Id { get; set; }
    public string OrderNumber { get; set; }
    public string? CustomerId { get; set; }
    public string? CustomerName { get; set; }
    public string? CustomerPhone { get; set; }
    public string StoreId { get; set; }
    public string StaffId { get; set; }
    public string StaffName { get; set; }
    public string Status { get; set; }
    public List<OrderItemDto> Items { get; set; } = new List<OrderItemDto>();
    public decimal Subtotal { get; set; }
    public decimal Tax { get; set; }
    public decimal Discount { get; set; }
    public decimal Tip { get; set; }
    public decimal Total { get; set; }
    public string PaymentMethod { get; set; }
    public string PaymentStatus { get; set; }
    public string? Notes { get; set; }
    public DateTime CreatedAt { get; set; }
    public DateTime UpdatedAt { get; set; }
}

// CreateOrderDto.cs
public class CreateOrderDto
{
    public string? CustomerName { get; set; }
    public string? CustomerPhone { get; set; }
    public string? CustomerId { get; set; }
    
    [Required]
    [MinLength(1)]
    public List<CreateOrderItemDto> Items { get; set; } = new List<CreateOrderItemDto>();
    
    [Range(0, 999999.99)]
    public decimal Discount { get; set; } = 0;
    
    [Range(0, 999999.99)]
    public decimal Tip { get; set; } = 0;
    
    [Required]
    public string PaymentMethod { get; set; }
    
    public string? Notes { get; set; }
}

// OrderItemDto.cs
public class OrderItemDto
{
    public string Id { get; set; }
    public string ProductId { get; set; }
    public string ProductName { get; set; }
    public int Quantity { get; set; }
    public decimal Price { get; set; }
    public decimal Discount { get; set; }
    public decimal Total { get; set; }
}

6.5. AUTOMAPPER PROFILE

// AutoMapperProfile.cs
public class AutoMapperProfile : Profile
{
    public AutoMapperProfile()
    {
        // User mappings
        CreateMap<User, UserDto>()
            .ForMember(dest => dest.Role, opt => opt.MapFrom(src => src.Role.ToString()));
        
        // Product mappings
        CreateMap<Product, ProductDto>();
        CreateMap<CreateProductDto, Product>()
            .ForMember(dest => dest.Id, opt => opt.MapFrom(src => Guid.NewGuid().ToString()));
        CreateMap<UpdateProductDto, Product>()
            .ForAllMembers(opts => opts.Condition((src, dest, srcMember) => srcMember != null));
        
        // Order mappings
        CreateMap<Order, OrderDto>()
            .ForMember(dest => dest.Status, opt => opt.MapFrom(src => src.Status.ToString()))
            .ForMember(dest => dest.PaymentMethod, opt => opt.MapFrom(src => src.PaymentMethod.ToString()))
            .ForMember(dest => dest.StaffName, opt => opt.MapFrom(src => src.Staff.Name));
        
        CreateMap<OrderItem, OrderItemDto>()
            .ForMember(dest => dest.ProductName, opt => opt.MapFrom(src => src.Product.Name));
        
        CreateMap<CreateOrderDto, Order>()
            .ForMember(dest => dest.Id, opt => opt.MapFrom(src => Guid.NewGuid().ToString()))
            .ForMember(dest => dest.OrderNumber, opt => opt.Ignore())
            .ForMember(dest => dest.Status, opt => opt.MapFrom(src => OrderStatus.Pending));
        
        // Inventory mappings
        CreateMap<InventoryItem, InventoryItemDto>()
            .ForMember(dest => dest.ProductName, opt => opt.MapFrom(src => src.Product.Name))
            .ForMember(dest => dest.ProductImage, opt => opt.MapFrom(src => src.Product.Image))
            .ForMember(dest => dest.ProductPrice, opt => opt.MapFrom(src => src.Product.Price));
        
        // Customer mappings (CRM)
        CreateMap<Customer, CustomerDto>()
            .ForMember(dest => dest.Status, opt => opt.MapFrom(src => src.Status.ToString()));
        
        // Employee mappings (HRM)
        CreateMap<Employee, EmployeeDto>()
            .ForMember(dest => dest.Status, opt => opt.MapFrom(src => src.Status.ToString()))
            .ForMember(dest => dest.ManagerName, opt => opt.MapFrom(src => src.Manager.Name));
    }
}

================================================================================
7. SERVICES VÀ BUSINESS LOGIC
================================================================================

7.1. IAUTH SERVICE

// IAuthService.cs
public interface IAuthService
{
    Task<ApiResponseDto<LoginResponseDto>> LoginAsync(string email, string password);
    Task<ApiResponseDto<LoginResponseDto>> RefreshTokenAsync(string refreshToken);
    Task<bool> LogoutAsync(string userId);
    Task<UserDto> GetCurrentUserAsync(string userId);
    Task<bool> ValidatePermissionAsync(string userId, string permission);
    Task<List<string>> GetUserPermissionsAsync(string userId);
}

// AuthService.cs
public class AuthService : IAuthService
{
    private readonly IUserRepository _userRepository;
    private readonly JwtTokenService _tokenService;
    private readonly ILogger<AuthService> _logger;
    private readonly IMapper _mapper;
    
    public AuthService(
        IUserRepository userRepository,
        JwtTokenService tokenService,
        ILogger<AuthService> logger,
        IMapper mapper)
    {
        _userRepository = userRepository;
        _tokenService = tokenService;
        _logger = logger;
        _mapper = mapper;
    }
    
    public async Task<ApiResponseDto<LoginResponseDto>> LoginAsync(string email, string password)
    {
        try
        {
            var user = await _userRepository.GetByEmailAsync(email);
            
            if (user == null || !VerifyPassword(password, user.PasswordHash))
            {
                return ApiResponseDto<LoginResponseDto>.ErrorResult("Invalid email or password");
            }
            
            if (!user.IsActive)
            {
                return ApiResponseDto<LoginResponseDto>.ErrorResult("Account is deactivated");
            }
            
            // Get user permissions
            var permissions = GetRolePermissions(user.Role);
            
            // Generate tokens
            var accessToken = _tokenService.GenerateToken(user, permissions);
            var refreshToken = _tokenService.GenerateRefreshToken();
            
            // Update last login
            user.LastLogin = DateTime.UtcNow;
            await _userRepository.UpdateAsync(user);
            
            // Prepare response
            var userDto = _mapper.Map<UserDto>(user);
            userDto.Permissions = permissions.ToList();
            
            var response = new LoginResponseDto
            {
                AccessToken = accessToken,
                RefreshToken = refreshToken,
                ExpiresAt = DateTime.UtcNow.AddHours(24),
                User = userDto,
                CurrentStore = _mapper.Map<StoreDto>(user.Store),
                AvailableStores = await GetUserAvailableStores(user.Id)
            };
            
            _logger.LogInformation("User {Email} logged in successfully", email);
            
            return ApiResponseDto<LoginResponseDto>.SuccessResult(response, "Login successful");
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error during login for email {Email}", email);
            return ApiResponseDto<LoginResponseDto>.ErrorResult("An error occurred during login");
        }
    }
    
    public async Task<UserDto> GetCurrentUserAsync(string userId)
    {
        var user = await _userRepository.GetByIdWithStoreAsync(userId);
        if (user == null)
            return null;
        
        var userDto = _mapper.Map<UserDto>(user);
        userDto.Permissions = GetRolePermissions(user.Role).ToList();
        
        return userDto;
    }
    
    private bool VerifyPassword(string password, string hash)
    {
        return BCrypt.Net.BCrypt.Verify(password, hash);
    }
    
    private IEnumerable<string> GetRolePermissions(UserRole role)
    {
        return role switch
        {
            UserRole.Staff => RolePermissions.StaffPermissions,
            UserRole.Manager => RolePermissions.ManagerPermissions,
            UserRole.Admin => RolePermissions.AdminPermissions,
            _ => new List<string>()
        };
    }
}

7.2. IPRODUCT SERVICE

// IProductService.cs
public interface IProductService
{
    Task<PagedResultDto<ProductDto>> GetProductsAsync(string storeId, int page, int pageSize, 
        string? search = null, string? category = null, bool? isActive = null);
    Task<ProductDto?> GetProductByIdAsync(string id, string storeId);
    Task<ProductDto?> GetProductByBarcodeAsync(string barcode, string storeId);
    Task<ProductDto> CreateProductAsync(CreateProductDto request, string storeId, string userId);
    Task<ProductDto?> UpdateProductAsync(string id, UpdateProductDto request, string storeId, string userId);
    Task<bool> DeleteProductAsync(string id, string storeId, string userId);
    Task<List<string>> GetCategoriesAsync(string storeId);
}

// ProductService.cs
public class ProductService : IProductService
{
    private readonly IProductRepository _productRepository;
    private readonly IInventoryRepository _inventoryRepository;
    private readonly IMapper _mapper;
    private readonly ILogger<ProductService> _logger;
    
    public ProductService(
        IProductRepository productRepository,
        IInventoryRepository inventoryRepository,
        IMapper mapper,
        ILogger<ProductService> logger)
    {
        _productRepository = productRepository;
        _inventoryRepository = inventoryRepository;
        _mapper = mapper;
        _logger = logger;
    }
    
    public async Task<PagedResultDto<ProductDto>> GetProductsAsync(string storeId, int page, int pageSize, 
        string? search = null, string? category = null, bool? isActive = null)
    {
        var (products, totalCount) = await _productRepository.GetPagedAsync(
            storeId, page, pageSize, search, category, isActive);
        
        var productDtos = _mapper.Map<List<ProductDto>>(products);
        
        return new PagedResultDto<ProductDto>
        {
            Items = productDtos,
            TotalCount = totalCount,
            Page = page,
            PageSize = pageSize
        };
    }
    
    public async Task<ProductDto> CreateProductAsync(CreateProductDto request, string storeId, string userId)
    {
        using var transaction = await _productRepository.BeginTransactionAsync();
        
        try
        {
            // Check if barcode already exists
            var existingProduct = await _productRepository.GetByBarcodeAsync(request.Barcode, storeId);
            if (existingProduct != null)
            {
                throw new BusinessException("Product with this barcode already exists");
            }
            
            // Create product
            var product = _mapper.Map<Product>(request);
            product.CreatedBy = userId;
            
            await _productRepository.AddAsync(product);
            
            // Create initial inventory entry
            var inventoryItem = new InventoryItem
            {
                ProductId = product.Id,
                StoreId = storeId,
                Quantity = request.InitialQuantity,
                MinStock = request.MinStock,
                MaxStock = request.MaxStock,
                CreatedBy = userId
            };
            
            await _inventoryRepository.AddAsync(inventoryItem);
            
            await transaction.CommitAsync();
            
            _logger.LogInformation("Product {ProductName} created successfully by user {UserId}", 
                request.Name, userId);
            
            return _mapper.Map<ProductDto>(product);
        }
        catch
        {
            await transaction.RollbackAsync();
            throw;
        }
    }
}

7.3. IORDER SERVICE

// IOrderService.cs
public interface IOrderService
{
    Task<PagedResultDto<OrderDto>> GetOrdersAsync(string storeId, string? staffId, int page, int pageSize,
        string? search = null, OrderStatus? status = null, DateTime? startDate = null, DateTime? endDate = null);
    Task<OrderDto?> GetOrderByIdAsync(string id, string storeId);
    Task<OrderDto> CreateOrderAsync(CreateOrderDto request, string storeId, string userId);
    Task<OrderDto?> UpdateOrderAsync(string id, UpdateOrderDto request, string storeId, string userId);
    Task<bool> CompleteOrderAsync(string id, string storeId, string userId);
    Task<bool> CancelOrderAsync(string id, string reason, string storeId, string userId);
    Task<string> GenerateOrderNumberAsync(string storeId);
}

// OrderService.cs
public class OrderService : IOrderService
{
    private readonly IOrderRepository _orderRepository;
    private readonly IProductRepository _productRepository;
    private readonly IInventoryRepository _inventoryRepository;
    private readonly IMapper _mapper;
    private readonly ILogger<OrderService> _logger;
    
    public OrderService(
        IOrderRepository orderRepository,
        IProductRepository productRepository,
        IInventoryRepository inventoryRepository,
        IMapper mapper,
        ILogger<OrderService> logger)
    {
        _orderRepository = orderRepository;
        _productRepository = productRepository;
        _inventoryRepository = inventoryRepository;
        _mapper = mapper;
        _logger = logger;
    }
    
    public async Task<OrderDto> CreateOrderAsync(CreateOrderDto request, string storeId, string userId)
    {
        using var transaction = await _orderRepository.BeginTransactionAsync();
        
        try
        {
            // Validate products and check stock
            var productIds = request.Items.Select(i => i.ProductId).ToList();
            var products = await _productRepository.GetByIdsAsync(productIds);
            var inventoryItems = await _inventoryRepository.GetByProductIdsAsync(productIds, storeId);
            
            // Validate stock availability
            foreach (var item in request.Items)
            {
                var product = products.FirstOrDefault(p => p.Id == item.ProductId);
                if (product == null)
                {
                    throw new BusinessException($"Product with ID {item.ProductId} not found");
                }
                
                var inventory = inventoryItems.FirstOrDefault(i => i.ProductId == item.ProductId);
                if (inventory == null || inventory.Quantity < item.Quantity)
                {
                    throw new BusinessException($"Insufficient stock for product {product.Name}");
                }
            }
            
            // Create order
            var order = _mapper.Map<Order>(request);
            order.OrderNumber = await GenerateOrderNumberAsync(storeId);
            order.StoreId = storeId;
            order.StaffId = userId;
            order.CreatedBy = userId;
            
            // Calculate totals
            decimal subtotal = 0;
            var orderItems = new List<OrderItem>();
            
            foreach (var itemRequest in request.Items)
            {
                var product = products.First(p => p.Id == itemRequest.ProductId);
                var itemTotal = (product.Price * itemRequest.Quantity) - itemRequest.Discount;
                
                var orderItem = new OrderItem
                {
                    Id = Guid.NewGuid().ToString(),
                    ProductId = itemRequest.ProductId,
                    Quantity = itemRequest.Quantity,
                    Price = product.Price,
                    Discount = itemRequest.Discount,
                    Total = itemTotal
                };
                
                orderItems.Add(orderItem);
                subtotal += itemTotal;
            }
            
            order.Items = orderItems;
            order.Subtotal = subtotal;
            order.Tax = subtotal * 0.1m; // 10% tax
            order.Total = order.Subtotal + order.Tax + request.Tip - request.Discount;
            
            await _orderRepository.AddAsync(order);
            
            // Update inventory
            foreach (var item in request.Items)
            {
                await _inventoryRepository.ReduceStockAsync(item.ProductId, storeId, item.Quantity, userId);
            }
            
            await transaction.CommitAsync();
            
            _logger.LogInformation("Order {OrderNumber} created successfully by user {UserId}", 
                order.OrderNumber, userId);
            
            // Load full order with navigation properties
            var createdOrder = await _orderRepository.GetByIdWithDetailsAsync(order.Id);
            return _mapper.Map<OrderDto>(createdOrder);
        }
        catch
        {
            await transaction.RollbackAsync();
            throw;
        }
    }
    
    public async Task<string> GenerateOrderNumberAsync(string storeId)
    {
        var today = DateTime.UtcNow.Date;
        var dailyCount = await _orderRepository.GetDailyOrderCountAsync(storeId, today);
        
        return $"ORD-{DateTime.UtcNow:yyyyMMdd}-{dailyCount + 1:D3}";
    }
}

================================================================================
8. REAL-TIME FEATURES (SIGNALR)
================================================================================

8.1. NOTIFICATION HUB

// NotificationHub.cs
[Authorize]
public class NotificationHub : Hub
{
    private readonly ILogger<NotificationHub> _logger;
    
    public NotificationHub(ILogger<NotificationHub> logger)
    {
        _logger = logger;
    }
    
    public override async Task OnConnectedAsync()
    {
        var storeId = Context.User?.FindFirst("storeId")?.Value;
        var userId = Context.User?.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        if (!string.IsNullOrEmpty(storeId))
        {
            await Groups.AddToGroupAsync(Context.ConnectionId, $"store_{storeId}");
            _logger.LogInformation("User {UserId} connected to store group {StoreId}", userId, storeId);
        }
        
        if (!string.IsNullOrEmpty(userId))
        {
            await Groups.AddToGroupAsync(Context.ConnectionId, $"user_{userId}");
        }
        
        await base.OnConnectedAsync();
    }
    
    public override async Task OnDisconnectedAsync(Exception? exception)
    {
        var storeId = Context.User?.FindFirst("storeId")?.Value;
        var userId = Context.User?.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        
        if (!string.IsNullOrEmpty(storeId))
        {
            await Groups.RemoveFromGroupAsync(Context.ConnectionId, $"store_{storeId}");
        }
        
        if (!string.IsNullOrEmpty(userId))
        {
            await Groups.RemoveFromGroupAsync(Context.ConnectionId, $"user_{userId}");
        }
        
        _logger.LogInformation("User {UserId} disconnected", userId);
        await base.OnDisconnectedAsync(exception);
    }
    
    // Client can join specific groups
    public async Task JoinGroup(string groupName)
    {
        await Groups.AddToGroupAsync(Context.ConnectionId, groupName);
    }
    
    public async Task LeaveGroup(string groupName)
    {
        await Groups.RemoveFromGroupAsync(Context.ConnectionId, groupName);
    }
}

8.2. NOTIFICATION SERVICE

// INotificationService.cs
public interface INotificationService
{
    Task SendNotificationAsync(string userId, NotificationDto notification);
    Task SendBroadcastAsync(string storeId, NotificationDto notification);
    Task<PagedResultDto<NotificationDto>> GetUserNotificationsAsync(string userId, int page, int pageSize);
    Task MarkAsReadAsync(string notificationId, string userId);
    Task MarkAllAsReadAsync(string userId);
}

// NotificationService.cs
public class NotificationService : INotificationService
{
    private readonly INotificationRepository _notificationRepository;
    private readonly IHubContext<NotificationHub> _hubContext;
    private readonly IMapper _mapper;
    private readonly ILogger<NotificationService> _logger;
    
    public NotificationService(
        INotificationRepository notificationRepository,
        IHubContext<NotificationHub> hubContext,
        IMapper mapper,
        ILogger<NotificationService> logger)
    {
        _notificationRepository = notificationRepository;
        _hubContext = hubContext;
        _mapper = mapper;
        _logger = logger;
    }
    
    public async Task SendNotificationAsync(string userId, NotificationDto notification)
    {
        try
        {
            // Save to database
            var notificationEntity = _mapper.Map<Notification>(notification);
            notificationEntity.UserId = userId;
            
            await _notificationRepository.AddAsync(notificationEntity);
            
            // Send real-time notification
            await _hubContext.Clients.Group($"user_{userId}")
                .SendAsync("ReceiveNotification", notification);
            
            _logger.LogInformation("Notification sent to user {UserId}: {Title}", userId, notification.Title);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error sending notification to user {UserId}", userId);
        }
    }
    
    public async Task SendBroadcastAsync(string storeId, NotificationDto notification)
    {
        try
        {
            // Get all users in the store
            var storeUsers = await _notificationRepository.GetStoreUsersAsync(storeId);
            
            // Save notifications for all users
            var notifications = storeUsers.Select(user => new Notification
            {
                Id = Guid.NewGuid().ToString(),
                UserId = user.Id,
                Type = notification.Type,
                Title = notification.Title,
                Message = notification.Message,
                Data = notification.Data,
                CreatedAt = DateTime.UtcNow
            }).ToList();
            
            await _notificationRepository.AddRangeAsync(notifications);
            
            // Send real-time broadcast
            await _hubContext.Clients.Group($"store_{storeId}")
                .SendAsync("ReceiveBroadcast", notification);
            
            _logger.LogInformation("Broadcast sent to store {StoreId}: {Title}", storeId, notification.Title);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error sending broadcast to store {StoreId}", storeId);
        }
    }
}

================================================================================
9. FILE UPLOAD & STORAGE
================================================================================

9.1. FILE STORAGE SERVICE

// IFileStorageService.cs
public interface IFileStorageService
{
    Task<string> UploadFileAsync(IFormFile file, string folder);
    Task<bool> DeleteFileAsync(string filePath);
    Task<FileStream> GetFileStreamAsync(string filePath);
    string GetFileUrl(string filePath);
}

// FileStorageService.cs
public class FileStorageService : IFileStorageService
{
    private readonly IConfiguration _configuration;
    private readonly IWebHostEnvironment _environment;
    private readonly ILogger<FileStorageService> _logger;
    
    public FileStorageService(
        IConfiguration configuration,
        IWebHostEnvironment environment,
        ILogger<FileStorageService> logger)
    {
        _configuration = configuration;
        _environment = environment;
        _logger = logger;
    }
    
    public async Task<string> UploadFileAsync(IFormFile file, string folder)
    {
        if (file == null || file.Length == 0)
            throw new ArgumentException("File is required");
        
        // Validate file type
        var allowedExtensions = new[] { ".jpg", ".jpeg", ".png", ".gif", ".pdf", ".xlsx", ".docx" };
        var extension = Path.GetExtension(file.FileName).ToLowerInvariant();
        
        if (!allowedExtensions.Contains(extension))
            throw new ArgumentException($"File type {extension} is not allowed");
        
        // Validate file size (10MB max)
        if (file.Length > 10 * 1024 * 1024)
            throw new ArgumentException("File size cannot exceed 10MB");
        
        // Generate unique filename
        var fileName = $"{Guid.NewGuid()}{extension}";
        var uploadPath = Path.Combine(_environment.WebRootPath, "uploads", folder);
        
        // Create directory if it doesn't exist
        Directory.CreateDirectory(uploadPath);
        
        var filePath = Path.Combine(uploadPath, fileName);
        
        try
        {
            using var stream = new FileStream(filePath, FileMode.Create);
            await file.CopyToAsync(stream);
            
            var relativePath = Path.Combine("uploads", folder, fileName).Replace("\\", "/");
            
            _logger.LogInformation("File uploaded successfully: {FilePath}", relativePath);
            
            return relativePath;
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error uploading file: {FileName}", file.FileName);
            throw;
        }
    }
    
    public string GetFileUrl(string filePath)
    {
        var baseUrl = _configuration["AppSettings:BaseUrl"];
        return $"{baseUrl}/{filePath}";
    }
}

9.2. FILES CONTROLLER

// FilesController.cs
[ApiController]
[Route("api/[controller]")]
[Authorize]
public class FilesController : ControllerBase
{
    private readonly IFileStorageService _fileStorageService;
    private readonly ILogger<FilesController> _logger;
    
    public FilesController(IFileStorageService fileStorageService, ILogger<FilesController> logger)
    {
        _fileStorageService = fileStorageService;
        _logger = logger;
    }
    
    [HttpPost("upload")]
    public async Task<ActionResult<FileUploadResponseDto>> UploadFile(
        IFormFile file, 
        [FromQuery] string folder = "general")
    {
        try
        {
            var filePath = await _fileStorageService.UploadFileAsync(file, folder);
            var fileUrl = _fileStorageService.GetFileUrl(filePath);
            
            var response = new FileUploadResponseDto
            {
                FileName = file.FileName,
                FilePath = filePath,
                FileUrl = fileUrl,
                FileSize = file.Length,
                ContentType = file.ContentType,
                UploadedAt = DateTime.UtcNow
            };
            
            return Ok(response);
        }
        catch (ArgumentException ex)
        {
            return BadRequest(new { message = ex.Message });
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error uploading file");
            return StatusCode(500, new { message = "An error occurred while uploading the file" });
        }
    }
    
    [HttpPost("upload/multiple")]
    public async Task<ActionResult<List<FileUploadResponseDto>>> UploadMultipleFiles(
        List<IFormFile> files,
        [FromQuery] string folder = "general")
    {
        var responses = new List<FileUploadResponseDto>();
        
        foreach (var file in files)
        {
            try
            {
                var filePath = await _fileStorageService.UploadFileAsync(file, folder);
                var fileUrl = _fileStorageService.GetFileUrl(filePath);
                
                responses.Add(new FileUploadResponseDto
                {
                    FileName = file.FileName,
                    FilePath = filePath,
                    FileUrl = fileUrl,
                    FileSize = file.Length,
                    ContentType = file.ContentType,
                    UploadedAt = DateTime.UtcNow
                });
            }
            catch (Exception ex)
            {
                _logger.LogError(ex, "Error uploading file {FileName}", file.FileName);
                responses.Add(new FileUploadResponseDto
                {
                    FileName = file.FileName,
                    Error = ex.Message
                });
            }
        }
        
        return Ok(responses);
    }
    
    [HttpGet("download/{*filePath}")]
    public async Task<IActionResult> DownloadFile(string filePath)
    {
        try
        {
            var fileStream = await _fileStorageService.GetFileStreamAsync(filePath);
            var fileName = Path.GetFileName(filePath);
            var contentType = GetContentType(fileName);
            
            return File(fileStream, contentType, fileName);
        }
        catch (FileNotFoundException)
        {
            return NotFound();
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error downloading file {FilePath}", filePath);
            return StatusCode(500, new { message = "An error occurred while downloading the file" });
        }
    }
    
    private string GetContentType(string fileName)
    {
        var extension = Path.GetExtension(fileName).ToLowerInvariant();
        return extension switch
        {
            ".jpg" or ".jpeg" => "image/jpeg",
            ".png" => "image/png",
            ".gif" => "image/gif",
            ".pdf" => "application/pdf",
            ".xlsx" => "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
            ".docx" => "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
            _ => "application/octet-stream"
        };
    }
}

================================================================================
10. CACHING VÀ PERFORMANCE
================================================================================

10.1. REDIS CACHING SERVICE

// ICacheService.cs
public interface ICacheService
{
    Task<T?> GetAsync<T>(string key);
    Task SetAsync<T>(string key, T value, TimeSpan? expiration = null);
    Task RemoveAsync(string key);
    Task RemoveByPatternAsync(string pattern);
}

// RedisCacheService.cs
public class RedisCacheService : ICacheService
{
    private readonly IDatabase _database;
    private readonly ILogger<RedisCacheService> _logger;
    
    public RedisCacheService(IConnectionMultiplexer redis, ILogger<RedisCacheService> logger)
    {
        _database = redis.GetDatabase();
        _logger = logger;
    }
    
    public async Task<T?> GetAsync<T>(string key)
    {
        try
        {
            var value = await _database.StringGetAsync(key);
            if (!value.HasValue)
                return default;
            
            return JsonSerializer.Deserialize<T>(value);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error getting cache key: {Key}", key);
            return default;
        }
    }
    
    public async Task SetAsync<T>(string key, T value, TimeSpan? expiration = null)
    {
        try
        {
            var json = JsonSerializer.Serialize(value);
            await _database.StringSetAsync(key, json, expiration);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error setting cache key: {Key}", key);
        }
    }
    
    public async Task RemoveAsync(string key)
    {
        try
        {
            await _database.KeyDeleteAsync(key);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Error removing cache key: {Key}", key);
        }
    }
}

10.2. CACHE KEYS

// CacheKeys.cs
public static class CacheKeys
{
    public const string UserPermissions = "user_permissions_{0}";
    public const string ProductsByStore = "products_store_{0}";
    public const string InventoryByStore = "inventory_store_{0}";
    public const string DashboardAnalytics = "analytics_dashboard_{0}_{1}"; // storeId, date
    public const string CustomersByStore = "customers_store_{0}";
    
    public static string UserPermissionsKey(string userId) => string.Format(UserPermissions, userId);
    public static string ProductsByStoreKey(string storeId) => string.Format(ProductsByStore, storeId);
    public static string InventoryByStoreKey(string storeId) => string.Format(InventoryByStore, storeId);
    public static string DashboardAnalyticsKey(string storeId, string date) => 
        string.Format(DashboardAnalytics, storeId, date);
}

================================================================================
11. CONFIGURATION VÀ DEPLOYMENT
================================================================================

11.1. PROGRAM.CS

// Program.cs
var builder = WebApplication.CreateBuilder(args);

// Add services to the container
builder.Services.AddControllers();
builder.Services.AddEndpointsApiExplorer();
builder.Services.AddSwaggerGen();

// Database
builder.Services.AddDbContext<ApplicationDbContext>(options =>
    options.UseSqlServer(builder.Configuration.GetConnectionString("DefaultConnection")));

// Authentication
builder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
    .AddJwtBearer(options =>
    {
        options.TokenValidationParameters = new TokenValidationParameters
        {
            ValidateIssuer = true,
            ValidateAudience = true,
            ValidateLifetime = true,
            ValidateIssuerSigningKey = true,
            ValidIssuer = builder.Configuration["Jwt:Issuer"],
            ValidAudience = builder.Configuration["Jwt:Audience"],
            IssuerSigningKey = new SymmetricSecurityKey(
                Encoding.UTF8.GetBytes(builder.Configuration["Jwt:Key"]))
        };
        
        // SignalR authentication
        options.Events = new JwtBearerEvents
        {
            OnMessageReceived = context =>
            {
                var accessToken = context.Request.Query["access_token"];
                var path = context.HttpContext.Request.Path;
                
                if (!string.IsNullOrEmpty(accessToken) && path.StartsWithSegments("/hubs"))
                {
                    context.Token = accessToken;
                }
                
                return Task.CompletedTask;
            }
        };
    });

// Authorization
builder.Services.AddAuthorization();

// CORS
builder.Services.AddCors(options =>
{
    options.AddPolicy("AllowFrontend", policy =>
    {
        policy.WithOrigins(builder.Configuration.GetSection("AllowedOrigins").Get<string[]>())
              .AllowAnyHeader()
              .AllowAnyMethod()
              .AllowCredentials();
    });
});

// SignalR
builder.Services.AddSignalR();

// Redis Cache
builder.Services.AddStackExchangeRedisCache(options =>
{
    options.Configuration = builder.Configuration.GetConnectionString("Redis");
});

// AutoMapper
builder.Services.AddAutoMapper(typeof(AutoMapperProfile));

// FluentValidation
builder.Services.AddFluentValidationAutoValidation();
builder.Services.AddFluentValidationClientsideAdapters();
builder.Services.AddValidatorsFromAssemblyContaining<CreateProductValidator>();

// Dependency Injection
builder.Services.AddScoped<IUserRepository, UserRepository>();
builder.Services.AddScoped<IProductRepository, ProductRepository>();
builder.Services.AddScoped<IOrderRepository, OrderRepository>();
builder.Services.AddScoped<IInventoryRepository, InventoryRepository>();

builder.Services.AddScoped<IAuthService, AuthService>();
builder.Services.AddScoped<IUserService, UserService>();
builder.Services.AddScoped<IProductService, ProductService>();
builder.Services.AddScoped<IOrderService, OrderService>();
builder.Services.AddScoped<IInventoryService, InventoryService>();
builder.Services.AddScoped<IAnalyticsService, AnalyticsService>();
builder.Services.AddScoped<ICRMService, CRMService>();
builder.Services.AddScoped<ILRMService, LRMService>();
builder.Services.AddScoped<IHRMService, HRMService>();
builder.Services.AddScoped<INotificationService, NotificationService>();
builder.Services.AddScoped<IFileStorageService, FileStorageService>();

builder.Services.AddScoped<JwtTokenService>();
builder.Services.AddScoped<ICacheService, RedisCacheService>();

var app = builder.Build();

// Configure the HTTP request pipeline
if (app.Environment.IsDevelopment())
{
    app.UseSwagger();
    app.UseSwaggerUI();
}

app.UseHttpsRedirection();
app.UseStaticFiles();

app.UseCors("AllowFrontend");

app.UseAuthentication();
app.UseAuthorization();

app.UseMiddleware<ExceptionMiddleware>();

app.MapControllers();
app.MapHub<NotificationHub>("/hubs/notifications");

// Seed data
using (var scope = app.Services.CreateScope())
{
    var context = scope.ServiceProvider.GetRequiredService<ApplicationDbContext>();
    await context.Database.MigrateAsync();
    
    var seeder = scope.ServiceProvider.GetRequiredService<DataSeeder>();
    await seeder.SeedAsync();
}

app.Run();

11.2. APPSETTINGS.JSON

{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=POSSystemDB;Trusted_Connection=true;MultipleActiveResultSets=true",
    "Redis": "localhost:6379"
  },
  "Jwt": {
    "Key": "your-super-secret-jwt-key-that-is-at-least-256-bits-long",
    "Issuer": "POSSystem.API",
    "Audience": "POSSystem.Frontend"
  },
  "AllowedOrigins": [
    "http://localhost:3000",
    "http://localhost:5173",
    "https://yourdomain.com"
  ],
  "AppSettings": {
    "BaseUrl": "https://localhost:7001",
    "MaxFileSize": 10485760,
    "AllowedFileTypes": [".jpg", ".jpeg", ".png", ".gif", ".pdf", ".xlsx", ".docx"]
  },
  "Email": {
    "SmtpServer": "smtp.gmail.com",
    "Port": 587,
    "Username": "your-email@gmail.com",
    "Password": "your-app-password",
    "FromEmail": "noreply@possystem.com",
    "FromName": "POS System"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}

11.3. DOCKER CONFIGURATION

# Dockerfile
FROM mcr.microsoft.com/dotnet/aspnet:8.0 AS base
WORKDIR /app
EXPOSE 80
EXPOSE 443

FROM mcr.microsoft.com/dotnet/sdk:8.0 AS build
ARG BUILD_CONFIGURATION=Release
WORKDIR /src
COPY ["POSSystem.API/POSSystem.API.csproj", "POSSystem.API/"]
COPY ["POSSystem.Core/POSSystem.Core.csproj", "POSSystem.Core/"]
COPY ["POSSystem.Infrastructure/POSSystem.Infrastructure.csproj", "POSSystem.Infrastructure/"]
COPY ["POSSystem.Application/POSSystem.Application.csproj", "POSSystem.Application/"]
RUN dotnet restore "POSSystem.API/POSSystem.API.csproj"
COPY . .
WORKDIR "/src/POSSystem.API"
RUN dotnet build "POSSystem.API.csproj" -c $BUILD_CONFIGURATION -o /app/build

FROM build AS publish
ARG BUILD_CONFIGURATION=Release
RUN dotnet publish "POSSystem.API.csproj" -c $BUILD_CONFIGURATION -o /app/publish /p:UseAppHost=false

FROM base AS final
WORKDIR /app
COPY --from=publish /app/publish .
ENTRYPOINT ["dotnet", "POSSystem.API.dll"]

# docker-compose.yml
version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:80"
      - "5001:443"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=POSSystemDB;User Id=sa;Password=YourPassword123!;TrustServerCertificate=true
      - ConnectionStrings__Redis=redis:6379
    depends_on:
      - sqlserver
      - redis
    volumes:
      - ./uploads:/app/wwwroot/uploads

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=YourPassword123!
    ports:
      - "1433:1433"
    volumes:
      - sqldata:/var/opt/mssql

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

volumes:
  sqldata:
  redisdata:

================================================================================
12. TESTING STRATEGY
================================================================================

12.1. UNIT TEST EXAMPLE

// ProductServiceTests.cs
public class ProductServiceTests
{
    private readonly Mock<IProductRepository> _productRepositoryMock;
    private readonly Mock<IInventoryRepository> _inventoryRepositoryMock;
    private readonly Mock<IMapper> _mapperMock;
    private readonly Mock<ILogger<ProductService>> _loggerMock;
    private readonly ProductService _productService;
    
    public ProductServiceTests()
    {
        _productRepositoryMock = new Mock<IProductRepository>();
        _inventoryRepositoryMock = new Mock<IInventoryRepository>();
        _mapperMock = new Mock<IMapper>();
        _loggerMock = new Mock<ILogger<ProductService>>();
        
        _productService = new ProductService(
            _productRepositoryMock.Object,
            _inventoryRepositoryMock.Object,
            _mapperMock.Object,
            _loggerMock.Object
        );
    }
    
    [Fact]
    public async Task CreateProductAsync_ValidRequest_ReturnsProductDto()
    {
        // Arrange
        var request = new CreateProductDto
        {
            Name = "Test Product",
            Barcode = "123456789",
            Price = 10.99m,
            Cost = 5.50m,
            Category = "Test Category",
            InitialQuantity = 100,
            MinStock = 10,
            MaxStock = 500
        };
        
        var product = new Product
        {
            Id = "product-1",
            Name = request.Name,
            Barcode = request.Barcode,
            Price = request.Price,
            Cost = request.Cost,
            Category = request.Category
        };
        
        var productDto = new ProductDto
        {
            Id = product.Id,
            Name = product.Name,
            Barcode = product.Barcode,
            Price = product.Price,
            Cost = product.Cost,
            Category = product.Category
        };
        
        _productRepositoryMock.Setup(x => x.GetByBarcodeAsync(request.Barcode, It.IsAny<string>()))
            .ReturnsAsync((Product?)null);
        
        _productRepositoryMock.Setup(x => x.BeginTransactionAsync())
            .ReturnsAsync(Mock.Of<IDbContextTransaction>());
        
        _mapperMock.Setup(x => x.Map<Product>(request))
            .Returns(product);
        
        _mapperMock.Setup(x => x.Map<ProductDto>(product))
            .Returns(productDto);
        
        // Act
        var result = await _productService.CreateProductAsync(request, "store-1", "user-1");
        
        // Assert
        Assert.NotNull(result);
        Assert.Equal(productDto.Id, result.Id);
        Assert.Equal(productDto.Name, result.Name);
        
        _productRepositoryMock.Verify(x => x.AddAsync(It.IsAny<Product>()), Times.Once);
        _inventoryRepositoryMock.Verify(x => x.AddAsync(It.IsAny<InventoryItem>()), Times.Once);
    }
}

12.2. INTEGRATION TEST EXAMPLE

// OrdersControllerIntegrationTests.cs
public class OrdersControllerIntegrationTests : IClassFixture<WebApplicationFactory<Program>>
{
    private readonly WebApplicationFactory<Program> _factory;
    private readonly HttpClient _client;
    
    public OrdersControllerIntegrationTests(WebApplicationFactory<Program> factory)
    {
        _factory = factory.WithWebHostBuilder(builder =>
        {
            builder.ConfigureServices(services =>
            {
                // Replace database with in-memory database for testing
                services.RemoveAll(typeof(DbContextOptions<ApplicationDbContext>));
                services.AddDbContext<ApplicationDbContext>(options =>
                {
                    options.UseInMemoryDatabase("TestDb");
                });
            });
        });
        
        _client = _factory.CreateClient();
    }
    
    [Fact]
    public async Task GetOrders_WithValidToken_ReturnsOrdersList()
    {
        // Arrange
        var token = await GetValidJwtTokenAsync();
        _client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Bearer", token);
        
        // Act
        var response = await _client.GetAsync("/api/orders");
        
        // Assert
        response.EnsureSuccessStatusCode();
        var content = await response.Content.ReadAsStringAsync();
        var result = JsonSerializer.Deserialize<PagedResultDto<OrderDto>>(content);
        
        Assert.NotNull(result);
        Assert.IsType<List<OrderDto>>(result.Items);
    }
    
    private async Task<string> GetValidJwtTokenAsync()
    {
        var loginRequest = new LoginRequestDto
        {
            Email = "staff@store.com",
            Password = "password"
        };
        
        var response = await _client.PostAsJsonAsync("/api/auth/login", loginRequest);
        response.EnsureSuccessStatusCode();
        
        var content = await response.Content.ReadAsStringAsync();
        var loginResponse = JsonSerializer.Deserialize<LoginResponseDto>(content);
        
        return loginResponse.AccessToken;
    }
}

================================================================================
TỔNG KẾT VÀ KHUYẾN NGHỊ
================================================================================

🎯 **PRIORITIES IMPLEMENTATION:**

**PHASE 1 (Tuần 1-2):**
- Setup project structure và dependencies
- Database setup với Entity Framework
- Authentication & Authorization (JWT)
- Basic CRUD cho Users, Products, Orders
- File upload functionality

**PHASE 2 (Tuần 3-4):**
- POS module với real-time features
- Inventory management
- Analytics dashboard
- Notification system (SignalR)

**PHASE 3 (Tuần 5-6):**
- CRM module implementation
- LRM module implementation
- HRM module implementation
- Performance optimization & caching

**PHASE 4 (Tuần 7-8):**
- Testing (Unit + Integration)
- Docker containerization
- Deployment configuration
- Documentation và API documentation

🔧 **DEVELOPMENT TOOLS:**
- Visual Studio 2022 hoặc VS Code
- SQL Server Management Studio
- Redis Desktop Manager
- Postman hoặc Swagger UI
- Docker Desktop

📚 **PACKAGES CẦN THIẾT:**
```xml
<PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" Version="8.0.0" />
<PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="8.0.0" />
<PackageReference Include="Microsoft.EntityFrameworkCore.Tools" Version="8.0.0" />
<PackageReference Include="AutoMapper.Extensions.Microsoft.DependencyInjection" Version="12.0.1" />
<PackageReference Include="FluentValidation.AspNetCore" Version="11.3.0" />
<PackageReference Include="Microsoft.AspNetCore.SignalR" Version="1.1.0" />
<PackageReference Include="StackExchange.Redis" Version="2.6.122" />
<PackageReference Include="BCrypt.Net-Next" Version="4.0.3" />
<PackageReference Include="System.IdentityModel.Tokens.Jwt" Version="7.0.3" />
<PackageReference Include="ClosedXML" Version="0.102.1" />
```

🚀 **DEPLOYMENT:**
- Azure App Service + Azure SQL Database
- Hoặc AWS EC2 + RDS  
- Hoặc Docker containers trên cloud provider
- CDN cho static files
- Redis Cache cho performance

📋 **SECURITY CHECKLIST:**
- JWT token validation
- Role-based permissions
- Input validation (FluentValidation)
- SQL injection protection (EF Core)
- File upload security
- CORS configuration
- HTTPS enforcement
- Rate limiting

================================================================================
13. COMPLETE API ENDPOINTS MAPPING
================================================================================

Dựa trên cấu trúc routing từ App.tsx và các pages hiện có, đây là mapping đầy đủ các API endpoints:

13.1. AUTHENTICATION ENDPOINTS
POST   /api/auth/login                     - User login
POST   /api/auth/refresh                   - Refresh token
POST   /api/auth/logout                    - User logout
GET    /api/auth/me                        - Get current user info
POST   /api/auth/change-password          - Change password
POST   /api/auth/forgot-password          - Forgot password
POST   /api/auth/reset-password           - Reset password

13.2. USER MANAGEMENT ENDPOINTS  
GET    /api/users                         - Get paginated users (Manager only)
GET    /api/users/{id}                    - Get user by ID
POST   /api/users                         - Create new user (Manager only)
PUT    /api/users/{id}                    - Update user (Manager only)
DELETE /api/users/{id}                    - Delete user (Manager only)
POST   /api/users/{id}/activate           - Activate/deactivate user
GET    /api/users/by-store/{storeId}      - Get users by store
PUT    /api/users/{id}/role               - Update user role (Manager only)
PUT    /api/users/{id}/store              - Transfer user to different store

13.3. STORE MANAGEMENT ENDPOINTS
GET    /api/stores                        - Get all stores
GET    /api/stores/{id}                   - Get store by ID
POST   /api/stores                        - Create new store (Admin only)
PUT    /api/stores/{id}                   - Update store (Manager only)
DELETE /api/stores/{id}                   - Delete store (Admin only)
GET    /api/stores/{id}/statistics        - Get store statistics

13.4. PRODUCT MANAGEMENT ENDPOINTS
GET    /api/products                      - Get paginated products
GET    /api/products/{id}                 - Get product by ID
GET    /api/products/barcode/{barcode}    - Get product by barcode (POS)
POST   /api/products                      - Create new product
PUT    /api/products/{id}                 - Update product
DELETE /api/products/{id}                - Delete product
GET    /api/products/categories           - Get product categories
POST   /api/products/bulk-import          - Bulk import products
GET    /api/products/export               - Export products to Excel

13.5. INVENTORY MANAGEMENT ENDPOINTS
GET    /api/inventory                     - Get paginated inventory
GET    /api/inventory/{productId}         - Get inventory for specific product
POST   /api/inventory/adjust              - Adjust stock levels
POST   /api/inventory/receive             - Receive inventory
GET    /api/inventory/low-stock           - Get low stock items
GET    /api/inventory/movements           - Get inventory movements history
POST   /api/inventory/count               - Physical inventory count
GET    /api/inventory/valuation           - Get inventory valuation report

13.6. ORDER MANAGEMENT ENDPOINTS
GET    /api/orders                        - Get paginated orders
GET    /api/orders/{id}                   - Get order by ID
POST   /api/orders                        - Create new order (POS)
PUT    /api/orders/{id}                   - Update order
DELETE /api/orders/{id}                   - Cancel order
POST   /api/orders/{id}/complete          - Complete order
POST   /api/orders/{id}/refund            - Refund order
GET    /api/orders/{id}/receipt           - Get order receipt
GET    /api/orders/daily-summary          - Get daily orders summary
POST   /api/orders/{id}/email-receipt     - Email receipt to customer

13.7. PAYMENT ENDPOINTS
POST   /api/payments/process              - Process payment
POST   /api/payments/refund               - Process refund
GET    /api/payments/methods              - Get available payment methods
POST   /api/payments/card                 - Process card payment
POST   /api/payments/cash                 - Process cash payment
GET    /api/payments/{orderId}/status     - Get payment status

13.8. SHIFT MANAGEMENT ENDPOINTS
GET    /api/shifts                        - Get paginated shifts
GET    /api/shifts/{id}                   - Get shift by ID
POST   /api/shifts/open                   - Open new shift
POST   /api/shifts/{id}/close             - Close shift
GET    /api/shifts/current                - Get current open shift
GET    /api/shifts/staff/{staffId}        - Get shifts for specific staff
PUT    /api/shifts/{id}                   - Update shift details

13.9. TASK MANAGEMENT ENDPOINTS
GET    /api/tasks                         - Get paginated tasks
GET    /api/tasks/{id}                    - Get task by ID
POST   /api/tasks                         - Create new task
PUT    /api/tasks/{id}                    - Update task
DELETE /api/tasks/{id}                    - Delete task
POST   /api/tasks/{id}/complete           - Mark task as complete
GET    /api/tasks/my-tasks                - Get current user's tasks
POST   /api/tasks/{id}/assign             - Assign task to user

13.10. ANALYTICS ENDPOINTS
GET    /api/analytics/dashboard           - Get dashboard analytics
GET    /api/analytics/revenue             - Get revenue report
GET    /api/analytics/sales               - Get sales report
GET    /api/analytics/staff-performance   - Get staff performance
GET    /api/analytics/product-performance - Get product performance
GET    /api/analytics/customer-insights   - Get customer insights
POST   /api/analytics/export              - Export analytics report
GET    /api/analytics/realtime            - Get real-time analytics

13.11. CRM ENDPOINTS
GET    /api/crm/customers                 - Get paginated customers
GET    /api/crm/customers/{id}            - Get customer by ID
POST   /api/crm/customers                 - Create new customer
PUT    /api/crm/customers/{id}            - Update customer
DELETE /api/crm/customers/{id}            - Delete customer
GET    /api/crm/customers/{id}/orders     - Get customer orders
GET    /api/crm/customers/{id}/interactions - Get customer interactions
POST   /api/crm/interactions              - Create customer interaction
PUT    /api/crm/interactions/{id}         - Update interaction
GET    /api/crm/analytics                 - Get CRM analytics

13.12. LRM (LOGISTICS) ENDPOINTS
GET    /api/lrm/suppliers                 - Get paginated suppliers
GET    /api/lrm/suppliers/{id}            - Get supplier by ID
POST   /api/lrm/suppliers                 - Create new supplier
PUT    /api/lrm/suppliers/{id}            - Update supplier
DELETE /api/lrm/suppliers/{id}            - Delete supplier
GET    /api/lrm/transfers                 - Get stock transfers
POST   /api/lrm/transfers                 - Create stock transfer
PUT    /api/lrm/transfers/{id}/approve    - Approve transfer
POST   /api/lrm/transfers/{id}/receive    - Receive transfer
GET    /api/lrm/shipping/{orderId}        - Get shipping status
PUT    /api/lrm/shipping/{orderId}        - Update shipping status

13.13. HRM ENDPOINTS
GET    /api/hrm/employees                 - Get paginated employees
GET    /api/hrm/employees/{id}            - Get employee by ID
POST   /api/hrm/employees                 - Create new employee
PUT    /api/hrm/employees/{id}            - Update employee
DELETE /api/hrm/employees/{id}            - Delete employee
GET    /api/hrm/leave-requests            - Get leave requests
POST   /api/hrm/leave-requests            - Submit leave request
PUT    /api/hrm/leave-requests/{id}       - Approve/reject leave
GET    /api/hrm/schedules                 - Get work schedules
POST   /api/hrm/schedules                 - Create work schedule
PUT    /api/hrm/schedules/{id}            - Update schedule
GET    /api/hrm/performance/{employeeId}  - Get employee performance

13.14. NOTIFICATION ENDPOINTS
GET    /api/notifications                 - Get user notifications
POST   /api/notifications                 - Send notification
PUT    /api/notifications/{id}/read       - Mark as read
PUT    /api/notifications/read-all        - Mark all as read
DELETE /api/notifications/{id}            - Delete notification
POST   /api/notifications/broadcast       - Send broadcast (Manager only)

13.15. FILE MANAGEMENT ENDPOINTS
POST   /api/files/upload                  - Upload single file
POST   /api/files/upload/multiple         - Upload multiple files
GET    /api/files/download/{*filePath}    - Download file
DELETE /api/files/{filePath}              - Delete file
GET    /api/files/{filePath}/info         - Get file info

================================================================================
14. FRONTEND-BACKEND INTEGRATION
================================================================================

14.1. API CLIENT SERVICE

Tạo service layer trong frontend để gọi API:

// services/apiClient.ts
import axios, { AxiosInstance, AxiosResponse } from 'axios';

class ApiClient {
  private client: AxiosInstance;
  
  constructor() {
    this.client = axios.create({
      baseURL: process.env.REACT_APP_API_URL || 'https://localhost:7001/api',
      timeout: 30000,
      headers: {
        'Content-Type': 'application/json',
      },
    });
    
    // Request interceptor for auth token
    this.client.interceptors.request.use(
      (config) => {
        const token = localStorage.getItem('accessToken');
        if (token) {
          config.headers.Authorization = `Bearer ${token}`;
        }
        return config;
      },
      (error) => Promise.reject(error)
    );
    
    // Response interceptor for token refresh
    this.client.interceptors.response.use(
      (response) => response,
      async (error) => {
        if (error.response?.status === 401) {
          await this.refreshToken();
          return this.client.request(error.config);
        }
        return Promise.reject(error);
      }
    );
  }
  
  async refreshToken() {
    const refreshToken = localStorage.getItem('refreshToken');
    if (refreshToken) {
      try {
        const response = await this.client.post('/auth/refresh', { refreshToken });
        const { accessToken, refreshToken: newRefreshToken } = response.data;
        
        localStorage.setItem('accessToken', accessToken);
        localStorage.setItem('refreshToken', newRefreshToken);
      } catch (error) {
        localStorage.clear();
        window.location.href = '/login';
      }
    }
  }
}

export const apiClient = new ApiClient();

14.2. BACKEND API RESPONSE FORMAT

Standardize tất cả API responses:

// Standardized API Response
public class ApiResponse<T>
{
    public bool Success { get; set; }
    public string Message { get; set; }
    public T? Data { get; set; }
    public List<string> Errors { get; set; } = new();
    public object? Metadata { get; set; }
    
    // For pagination
    public PaginationInfo? Pagination { get; set; }
}

public class PaginationInfo
{
    public int Page { get; set; }
    public int PageSize { get; set; }
    public int TotalItems { get; set; }
    public int TotalPages { get; set; }
    public bool HasNext { get; set; }
    public bool HasPrevious { get; set; }
}

14.3. ENVIRONMENT CONFIGURATION

Backend appsettings cho các environments:

// appsettings.Development.json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=POSSystemDB_Dev;Trusted_Connection=true",
    "Redis": "localhost:6379"
  },
  "Jwt": {
    "Key": "development-jwt-key-for-pos-system-minimum-256-bits",
    "Issuer": "POSSystem.API.Dev",
    "Audience": "POSSystem.Frontend.Dev"
  },
  "AllowedOrigins": [
    "http://localhost:3000",
    "http://localhost:5173"
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Information"
    }
  }
}

// appsettings.Production.json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=your-production-server;Database=POSSystemDB;User Id=pos_user;Password=your-password;Encrypt=true;TrustServerCertificate=false",
    "Redis": "your-redis-server:6379"
  },
  "Jwt": {
    "Key": "production-super-secret-jwt-key-at-least-256-bits-long",
    "Issuer": "POSSystem.API",
    "Audience": "POSSystem.Frontend"
  },
  "AllowedOrigins": [
    "https://yourdomain.com",
    "https://www.yourdomain.com"
  ]
}

================================================================================
15. THEME & LOCALIZATION SUPPORT
================================================================================

15.1. BACKEND LOCALIZATION SUPPORT

// Localization/ILocalizationService.cs
public interface ILocalizationService
{
    Task<Dictionary<string, string>> GetTranslationsAsync(string language);
    Task<string> GetTranslationAsync(string key, string language);
    Task SaveTranslationAsync(string key, string value, string language);
    Task<List<string>> GetSupportedLanguagesAsync();
}

// Controllers/LocalizationController.cs
[ApiController]
[Route("api/[controller]")]
public class LocalizationController : ControllerBase
{
    private readonly ILocalizationService _localizationService;
    
    public LocalizationController(ILocalizationService localizationService)
    {
        _localizationService = localizationService;
    }
    
    [HttpGet("translations/{language}")]
    public async Task<ActionResult<Dictionary<string, string>>> GetTranslations(string language)
    {
        var translations = await _localizationService.GetTranslationsAsync(language);
        return Ok(translations);
    }
    
    [HttpGet("languages")]
    public async Task<ActionResult<List<string>>> GetSupportedLanguages()
    {
        var languages = await _localizationService.GetSupportedLanguagesAsync();
        return Ok(languages);
    }
    
    [HttpPost("translations")]
    [RequirePermission(Permissions.ManageSettings)]
    public async Task<IActionResult> SaveTranslation([FromBody] SaveTranslationDto request)
    {
        await _localizationService.SaveTranslationAsync(request.Key, request.Value, request.Language);
        return Ok(new { message = "Translation saved successfully" });
    }
}

15.2. THEME CONFIGURATION ENDPOINTS

// Controllers/ThemeController.cs
[ApiController]
[Route("api/[controller]")]
[Authorize]
public class ThemeController : ControllerBase
{
    [HttpGet("user-preferences")]
    public async Task<ActionResult<UserThemePreferencesDto>> GetUserThemePreferences()
    {
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        // Return user's theme preferences
        return Ok(new UserThemePreferencesDto
        {
            Theme = "light", // or "dark"
            Language = "en", // or "vi"
            CompactMode = false,
            SidebarCollapsed = false
        });
    }
    
    [HttpPost("user-preferences")]
    public async Task<IActionResult> SaveUserThemePreferences([FromBody] UserThemePreferencesDto preferences)
    {
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        // Save user's theme preferences to database
        return Ok(new { message = "Theme preferences saved successfully" });
    }
}

15.3. STORE CUSTOMIZATION ENDPOINTS

// Controllers/CustomizationController.cs
[ApiController]
[Route("api/[controller]")]
[Authorize]
public class CustomizationController : ControllerBase
{
    [HttpGet("store-branding")]
    public async Task<ActionResult<StoreBrandingDto>> GetStoreBranding()
    {
        var storeId = User.FindFirst("storeId")?.Value;
        // Return store branding configuration
        return Ok(new StoreBrandingDto
        {
            Logo = "/uploads/store-logos/logo.png",
            PrimaryColor = "#030213",
            SecondaryColor = "#f3f3f5",
            StoreName = "Main Store",
            ReceiptHeader = "Thank you for shopping with us!",
            ReceiptFooter = "Visit us again soon!"
        });
    }
    
    [HttpPost("store-branding")]
    [RequirePermission(Permissions.ManageSettings)]
    public async Task<IActionResult> SaveStoreBranding([FromBody] StoreBrandingDto branding)
    {
        var storeId = User.FindFirst("storeId")?.Value;
        // Save store branding configuration
        return Ok(new { message = "Store branding saved successfully" });
    }
}

================================================================================
16. ERROR HANDLING & VALIDATION
================================================================================

16.1. GLOBAL EXCEPTION MIDDLEWARE

// Middleware/GlobalExceptionMiddleware.cs
public class GlobalExceptionMiddleware
{
    private readonly RequestDelegate _next;
    private readonly ILogger<GlobalExceptionMiddleware> _logger;
    
    public GlobalExceptionMiddleware(RequestDelegate next, ILogger<GlobalExceptionMiddleware> logger)
    {
        _next = next;
        _logger = logger;
    }
    
    public async Task InvokeAsync(HttpContext context)
    {
        try
        {
            await _next(context);
        }
        catch (Exception ex)
        {
            await HandleExceptionAsync(context, ex);
        }
    }
    
    private async Task HandleExceptionAsync(HttpContext context, Exception exception)
    {
        context.Response.ContentType = "application/json";
        
        var response = new ApiResponse<object>
        {
            Success = false,
            Message = "An error occurred while processing your request"
        };
        
        switch (exception)
        {
            case ValidationException validationEx:
                context.Response.StatusCode = 400;
                response.Message = "Validation failed";
                response.Errors = validationEx.Errors.Select(e => e.ErrorMessage).ToList();
                break;
                
            case BusinessException businessEx:
                context.Response.StatusCode = 400;
                response.Message = businessEx.Message;
                break;
                
            case UnauthorizedAccessException:
                context.Response.StatusCode = 401;
                response.Message = "Unauthorized access";
                break;
                
            case NotFoundException notFoundEx:
                context.Response.StatusCode = 404;
                response.Message = notFoundEx.Message;
                break;
                
            case ForbiddenException forbiddenEx:
                context.Response.StatusCode = 403;
                response.Message = forbiddenEx.Message;
                break;
                
            default:
                context.Response.StatusCode = 500;
                _logger.LogError(exception, "Unhandled exception occurred");
                
                if (Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT") == "Development")
                {
                    response.Message = exception.Message;
                    response.Errors = new List<string> { exception.StackTrace ?? "" };
                }
                break;
        }
        
        var jsonResponse = JsonSerializer.Serialize(response);
        await context.Response.WriteAsync(jsonResponse);
    }
}

16.2. CUSTOM EXCEPTIONS

// Exceptions/BusinessException.cs
public class BusinessException : Exception
{
    public BusinessException(string message) : base(message) { }
    public BusinessException(string message, Exception innerException) : base(message, innerException) { }
}

// Exceptions/NotFoundException.cs
public class NotFoundException : Exception
{
    public NotFoundException(string message) : base(message) { }
    public NotFoundException(string entityName, object key) 
        : base($"{entityName} with key '{key}' was not found") { }
}

// Exceptions/ForbiddenException.cs
public class ForbiddenException : Exception
{
    public ForbiddenException(string message) : base(message) { }
}

16.3. FLUENT VALIDATION RULES

// Validators/CreateOrderValidator.cs
public class CreateOrderValidator : AbstractValidator<CreateOrderDto>
{
    public CreateOrderValidator()
    {
        RuleFor(x => x.Items)
            .NotEmpty()
            .WithMessage("Order must contain at least one item");
            
        RuleFor(x => x.PaymentMethod)
            .NotEmpty()
            .Must(BeValidPaymentMethod)
            .WithMessage("Invalid payment method");
            
        RuleFor(x => x.Discount)
            .GreaterThanOrEqualTo(0)
            .WithMessage("Discount cannot be negative");
            
        RuleFor(x => x.Tip)
            .GreaterThanOrEqualTo(0)
            .WithMessage("Tip cannot be negative");
            
        RuleForEach(x => x.Items)
            .SetValidator(new CreateOrderItemValidator());
    }
    
    private bool BeValidPaymentMethod(string paymentMethod)
    {
        return Enum.TryParse<PaymentMethod>(paymentMethod, true, out _);
    }
}

// Validators/CreateOrderItemValidator.cs
public class CreateOrderItemValidator : AbstractValidator<CreateOrderItemDto>
{
    public CreateOrderItemValidator()
    {
        RuleFor(x => x.ProductId)
            .NotEmpty()
            .WithMessage("Product ID is required");
            
        RuleFor(x => x.Quantity)
            .GreaterThan(0)
            .WithMessage("Quantity must be greater than 0");
            
        RuleFor(x => x.Discount)
            .GreaterThanOrEqualTo(0)
            .WithMessage("Discount cannot be negative");
    }
}

================================================================================
17. MODAL SYSTEMS & UI COMPONENTS
================================================================================

17.1. BACKEND SUPPORT CHO MODAL OPERATIONS

Dựa trên các modal components trong frontend, backend cần support:

// Controllers/ModalsController.cs
[ApiController]
[Route("api/[controller]")]
[Authorize]
public class ModalsController : ControllerBase
{
    // Support cho AddProductModal
    [HttpGet("product-categories")]
    public async Task<ActionResult<List<string>>> GetProductCategories()
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var categories = await _productService.GetCategoriesAsync(storeId);
        return Ok(categories);
    }
    
    // Support cho OrderDetailsModal
    [HttpGet("order-actions/{orderId}")]
    public async Task<ActionResult<OrderActionsDto>> GetAvailableOrderActions(string orderId)
    {
        var userId = User.FindFirst(ClaimTypes.NameIdentifier)?.Value;
        var actions = await _orderService.GetAvailableActionsAsync(orderId, userId);
        return Ok(actions);
    }
    
    // Support cho CustomerInteractionModal
    [HttpGet("interaction-templates")]
    public async Task<ActionResult<List<InteractionTemplateDto>>> GetInteractionTemplates()
    {
        var templates = await _crmService.GetInteractionTemplatesAsync();
        return Ok(templates);
    }
    
    // Support cho CreateTaskModal
    [HttpGet("task-priorities")]
    public async Task<ActionResult<List<string>>> GetTaskPriorities()
    {
        return Ok(Enum.GetNames<TaskPriority>());
    }
    
    [HttpGet("assignable-users")]
    [RequirePermission(Permissions.AssignTasks)]
    public async Task<ActionResult<List<UserDto>>> GetAssignableUsers()
    {
        var storeId = User.FindFirst("storeId")?.Value;
        var users = await _userService.GetAssignableUsersAsync(storeId);
        return Ok(users);
    }
}

17.2. DROPDOWN DATA ENDPOINTS

// Controllers/DropdownDataController.cs
[ApiController]
[Route("api/[controller]")]
[Authorize]
public class DropdownDataController : ControllerBase
{
    [HttpGet("payment-methods")]
    public ActionResult<List<PaymentMethodDto>> GetPaymentMethods()
    {
        return Ok(new List<PaymentMethodDto>
        {
            new() { Value = "cash", Label = "Cash", Icon = "dollar-sign" },
            new() { Value = "card", Label = "Credit/Debit Card", Icon = "credit-card" },
            new() { Value = "digital", Label = "Digital Payment", Icon = "smartphone" }
        });
    }
    
    [HttpGet("order-statuses")]
    public ActionResult<List<DropdownItemDto>> GetOrderStatuses()
    {
        return Ok(new List<DropdownItemDto>
        {
            new() { Value = "pending", Label = "Pending" },
            new() { Value = "processing", Label = "Processing" },
            new() { Value = "completed", Label = "Completed" },
            new() { Value = "cancelled", Label = "Cancelled" },
            new() { Value = "refunded", Label = "Refunded" }
        });
    }
    
    [HttpGet("customer-statuses")]
    public ActionResult<List<DropdownItemDto>> GetCustomerStatuses()
    {
        return Ok(new List<DropdownItemDto>
        {
            new() { Value = "lead", Label = "Lead" },
            new() { Value = "customer", Label = "Customer" },
            new() { Value = "inactive", Label = "Inactive" }
        });
    }
    
    [HttpGet("leave-types")]
    public ActionResult<List<DropdownItemDto>> GetLeaveTypes()
    {
        return Ok(new List<DropdownItemDto>
        {
            new() { Value = "vacation", Label = "Vacation" },
            new() { Value = "sick", Label = "Sick Leave" },
            new() { Value = "personal", Label = "Personal" },
            new() { Value = "emergency", Label = "Emergency" }
        });
    }
}

================================================================================
18. HOOKS & UTILITIES INTEGRATION
================================================================================

18.1. PAGINATION HOOK SUPPORT

Backend pagination cần support usePagination hook:

// DTOs/PaginationRequestDto.cs
public class PaginationRequestDto
{
    public int Page { get; set; } = 1;
    public int PageSize { get; set; } = 10;
    public string? Search { get; set; }
    public string? SortBy { get; set; }
    public string? SortDirection { get; set; } = "asc";
    public Dictionary<string, string> Filters { get; set; } = new();
}

// DTOs/PaginatedResponseDto.cs
public class PaginatedResponseDto<T>
{
    public List<T> Items { get; set; } = new();
    public PaginationMetadata Pagination { get; set; } = new();
}

public class PaginationMetadata
{
    public int Page { get; set; }
    public int PageSize { get; set; }
    public int TotalItems { get; set; }
    public int TotalPages { get; set; }
    public bool HasNext { get; set; }
    public bool HasPrevious { get; set; }
    public int? NextPage { get; set; }
    public int? PreviousPage { get; set; }
}

18.2. SEARCH & FILTER SUPPORT

// Services/SearchService.cs
public class SearchService
{
    public static IQueryable<T> ApplySearch<T>(IQueryable<T> query, string? searchTerm, 
        params Expression<Func<T, string>>[] searchProperties)
    {
        if (string.IsNullOrEmpty(searchTerm) || !searchProperties.Any())
            return query;
        
        var parameter = Expression.Parameter(typeof(T));
        Expression? searchExpression = null;
        
        foreach (var property in searchProperties)
        {
            var propertyExpression = Expression.Invoke(property, parameter);
            var containsMethod = typeof(string).GetMethod("Contains", new[] { typeof(string) });
            var searchValue = Expression.Constant(searchTerm, typeof(string));
            var containsExpression = Expression.Call(propertyExpression, containsMethod!, searchValue);
            
            searchExpression = searchExpression == null 
                ? containsExpression 
                : Expression.OrElse(searchExpression, containsExpression);
        }
        
        if (searchExpression != null)
        {
            var lambda = Expression.Lambda<Func<T, bool>>(searchExpression, parameter);
            query = query.Where(lambda);
        }
        
        return query;
    }
    
    public static IQueryable<T> ApplySorting<T>(IQueryable<T> query, string? sortBy, string? sortDirection)
    {
        if (string.IsNullOrEmpty(sortBy))
            return query;
        
        var parameter = Expression.Parameter(typeof(T));
        var property = Expression.Property(parameter, sortBy);
        var lambda = Expression.Lambda(property, parameter);
        
        var methodName = sortDirection?.ToLower() == "desc" ? "OrderByDescending" : "OrderBy";
        var method = typeof(Queryable).GetMethods()
            .First(m => m.Name == methodName && m.GetParameters().Length == 2)
            .MakeGenericMethod(typeof(T), property.Type);
        
        return (IQueryable<T>)method.Invoke(null, new object[] { query, lambda })!;
    }
}

18.3. DATE FILTERING SUPPORT

// DTOs/DateRangeFilterDto.cs
public class DateRangeFilterDto
{
    public DateTime? StartDate { get; set; }
    public DateTime? EndDate { get; set; }
    public string? Period { get; set; } // "today", "week", "month", "year", "custom"
}

// Services/DateFilterService.cs
public class DateFilterService
{
    public static (DateTime StartDate, DateTime EndDate) GetDateRange(DateRangeFilterDto filter)
    {
        var now = DateTime.UtcNow;
        
        return filter.Period?.ToLower() switch
        {
            "today" => (now.Date, now.Date.AddDays(1).AddTicks(-1)),
            "week" => (now.Date.AddDays(-(int)now.DayOfWeek), now.Date.AddDays(7 - (int)now.DayOfWeek).AddTicks(-1)),
            "month" => (new DateTime(now.Year, now.Month, 1), new DateTime(now.Year, now.Month + 1, 1).AddTicks(-1)),
            "year" => (new DateTime(now.Year, 1, 1), new DateTime(now.Year + 1, 1, 1).AddTicks(-1)),
            "custom" => (filter.StartDate ?? now.Date.AddDays(-30), filter.EndDate ?? now.Date.AddDays(1).AddTicks(-1)),
            _ => (now.Date.AddDays(-30), now.Date.AddDays(1).AddTicks(-1))
        };
    }
}

================================================================================
19. PRODUCTION DEPLOYMENT GUIDE
================================================================================

19.1. AZURE DEPLOYMENT

// azure-pipelines.yml
trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'
  azureSubscription: 'your-azure-subscription'
  webAppName: 'pos-system-api'

steps:
- task: UseDotNet@2
  displayName: 'Use .NET 8.0'
  inputs:
    packageType: 'sdk'
    version: '8.0.x'

- task: DotNetCoreCLI@2
  displayName: 'Restore packages'
  inputs:
    command: 'restore'
    projects: '**/*.csproj'

- task: DotNetCoreCLI@2
  displayName: 'Build application'
  inputs:
    command: 'build'
    projects: '**/*.csproj'
    arguments: '--configuration $(buildConfiguration)'

- task: DotNetCoreCLI@2
  displayName: 'Run tests'
  inputs:
    command: 'test'
    projects: '**/*Tests.csproj'
    arguments: '--configuration $(buildConfiguration) --collect:"XPlat Code Coverage"'

- task: DotNetCoreCLI@2
  displayName: 'Publish application'
  inputs:
    command: 'publish'
    projects: '**/*.csproj'
    arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)'

- task: AzureWebApp@1
  displayName: 'Deploy to Azure Web App'
  inputs:
    azureSubscription: '$(azureSubscription)'
    appType: 'webApp'
    appName: '$(webAppName)'
    package: '$(Build.ArtifactStagingDirectory)/**/*.zip'

19.2. DOCKER PRODUCTION SETUP

// docker-compose.prod.yml
version: '3.8'

services:
  api:
    image: pos-system-api:latest
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "80:80"
      - "443:443"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=https://+:443;http://+:80
      - ConnectionStrings__DefaultConnection=${DATABASE_CONNECTION_STRING}
      - ConnectionStrings__Redis=redis:6379
      - Jwt__Key=${JWT_SECRET_KEY}
    depends_on:
      - sqlserver
      - redis
    volumes:
      - ./uploads:/app/wwwroot/uploads
      - ./certificates:/app/certificates
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=${SQL_SA_PASSWORD}
      - MSSQL_PID=Express
    ports:
      - "1433:1433"
    volumes:
      - sqldata:/var/opt/mssql
      - ./backups:/var/opt/mssql/backups
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./certificates:/etc/nginx/ssl
      - ./uploads:/var/www/uploads
    depends_on:
      - api
    restart: unless-stopped

volumes:
  sqldata:
  redisdata:

19.3. NGINX CONFIGURATION

// nginx.conf
events {
    worker_connections 1024;
}

http {
    upstream api {
        server api:80;
    }
    
    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
    
    server {
        listen 80;
        server_name yourdomain.com;
        
        # Redirect HTTP to HTTPS
        return 301 https://$server_name$request_uri;
    }
    
    server {
        listen 443 ssl http2;
        server_name yourdomain.com;
        
        ssl_certificate /etc/nginx/ssl/certificate.crt;
        ssl_certificate_key /etc/nginx/ssl/private.key;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        
        # Security headers
        add_header X-Frame-Options DENY;
        add_header X-Content-Type-Options nosniff;
        add_header X-XSS-Protection "1; mode=block";
        add_header Strict-Transport-Security "max-age=31536000; includeSubDomains";
        
        # API routes
        location /api/ {
            limit_req zone=api burst=20 nodelay;
            proxy_pass http://api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # SignalR WebSocket
        location /hubs/ {
            proxy_pass http://api;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }
        
        # Static files
        location /uploads/ {
            alias /var/www/uploads/;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
        
        # Frontend app
        location / {
            proxy_pass http://frontend:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}

19.4. HEALTH CHECKS

// HealthChecks/DatabaseHealthCheck.cs
public class DatabaseHealthCheck : IHealthCheck
{
    private readonly ApplicationDbContext _context;
    
    public DatabaseHealthCheck(ApplicationDbContext context)
    {
        _context = context;
    }
    
    public async Task<HealthCheckResult> CheckHealthAsync(HealthCheckContext context, 
        CancellationToken cancellationToken = default)
    {
        try
        {
            await _context.Database.CanConnectAsync(cancellationToken);
            return HealthCheckResult.Healthy("Database connection is healthy");
        }
        catch (Exception ex)
        {
            return HealthCheckResult.Unhealthy("Database connection failed", ex);
        }
    }
}

// Program.cs additions for health checks
builder.Services.AddHealthChecks()
    .AddCheck<DatabaseHealthCheck>("database")
    .AddRedis(builder.Configuration.GetConnectionString("Redis"))
    .AddSignalR();

// Health check endpoint
app.MapHealthChecks("/health", new HealthCheckOptions
{
    ResponseWriter = UIResponseWriter.WriteHealthCheckUIResponse
});

================================================================================
20. TROUBLESHOOTING & MONITORING
================================================================================

20.1. LOGGING CONFIGURATION

// Logging/SerilogConfiguration.cs
public static class SerilogConfiguration
{
    public static void ConfigureSerilog(this WebApplicationBuilder builder)
    {
        builder.Host.UseSerilog((context, config) =>
        {
            config
                .MinimumLevel.Information()
                .MinimumLevel.Override("Microsoft", LogEventLevel.Warning)
                .MinimumLevel.Override("Microsoft.Hosting.Lifetime", LogEventLevel.Information)
                .Enrich.FromLogContext()
                .Enrich.WithProperty("Application", "POSSystem.API")
                .WriteTo.Console(outputTemplate: 
                    "[{Timestamp:HH:mm:ss} {Level:u3}] {Message:lj} {Properties:j}{NewLine}{Exception}")
                .WriteTo.File("logs/pos-system-.log", 
                    rollingInterval: RollingInterval.Day,
                    retainedFileCountLimit: 30)
                .WriteTo.Seq(context.Configuration.GetConnectionString("Seq"))
                .ReadFrom.Configuration(context.Configuration);
        });
    }
}

20.2. APPLICATION INSIGHTS

// ApplicationInsights/TelemetryConfiguration.cs
public static class TelemetryConfiguration
{
    public static void ConfigureApplicationInsights(this IServiceCollection services, 
        IConfiguration configuration)
    {
        services.AddApplicationInsightsTelemetry(configuration);
        
        services.AddSingleton<ITelemetryInitializer, ApplicationTelemetryInitializer>();
        
        services.Configure<TelemetryConfiguration>(telemetryConfig =>
        {
            telemetryConfig.SetAzureTokenCredential(new DefaultAzureCredential());
        });
    }
}

20.3. PERFORMANCE MONITORING

// Middleware/PerformanceLoggingMiddleware.cs
public class PerformanceLoggingMiddleware
{
    private readonly RequestDelegate _next;
    private readonly ILogger<PerformanceLoggingMiddleware> _logger;
    
    public PerformanceLoggingMiddleware(RequestDelegate next, ILogger<PerformanceLoggingMiddleware> logger)
    {
        _next = next;
        _logger = logger;
    }
    
    public async Task InvokeAsync(HttpContext context)
    {
        var stopwatch = Stopwatch.StartNew();
        
        try
        {
            await _next(context);
        }
        finally
        {
            stopwatch.Stop();
            
            if (stopwatch.ElapsedMilliseconds > 1000) // Log slow requests
            {
                _logger.LogWarning("Slow request: {Method} {Path} took {ElapsedMs}ms",
                    context.Request.Method,
                    context.Request.Path,
                    stopwatch.ElapsedMilliseconds);
            }
        }
    }
}

20.4. ERROR TRACKING

// Services/ErrorTrackingService.cs
public interface IErrorTrackingService
{
    Task LogErrorAsync(Exception exception, string userId = null, Dictionary<string, object> context = null);
    Task LogWarningAsync(string message, string userId = null, Dictionary<string, object> context = null);
}

public class ErrorTrackingService : IErrorTrackingService
{
    private readonly ILogger<ErrorTrackingService> _logger;
    private readonly TelemetryClient _telemetryClient;
    
    public ErrorTrackingService(ILogger<ErrorTrackingService> logger, TelemetryClient telemetryClient)
    {
        _logger = logger;
        _telemetryClient = telemetryClient;
    }
    
    public async Task LogErrorAsync(Exception exception, string userId = null, Dictionary<string, object> context = null)
    {
        var properties = new Dictionary<string, string>();
        if (userId != null) properties["UserId"] = userId;
        if (context != null)
        {
            foreach (var kvp in context)
            {
                properties[kvp.Key] = kvp.Value?.ToString() ?? "";
            }
        }
        
        _telemetryClient.TrackException(exception, properties);
        _logger.LogError(exception, "Application error occurred. UserId: {UserId}, Context: {@Context}", 
            userId, context);
    }
    
    public async Task LogWarningAsync(string message, string userId = null, Dictionary<string, object> context = null)
    {
        _logger.LogWarning("Warning: {Message}. UserId: {UserId}, Context: {@Context}", 
            message, userId, context);
    }
}

20.5. MONITORING DASHBOARD

// Controllers/MonitoringController.cs
[ApiController]
[Route("api/[controller]")]
[Authorize]
[RequirePermission(Permissions.ViewAuditLog)]
public class MonitoringController : ControllerBase
{
    [HttpGet("system-health")]
    public async Task<ActionResult<SystemHealthDto>> GetSystemHealth()
    {
        return Ok(new SystemHealthDto
        {
            Status = "Healthy",
            Timestamp = DateTime.UtcNow,
            Services = new Dictionary<string, string>
            {
                ["Database"] = "Healthy",
                ["Redis"] = "Healthy",
                ["SignalR"] = "Healthy",
                ["FileStorage"] = "Healthy"
            },
            Metrics = new Dictionary<string, object>
            {
                ["ActiveConnections"] = 150,
                ["RequestsPerMinute"] = 1200,
                ["MemoryUsage"] = "65%",
                ["CpuUsage"] = "45%"
            }
        });
    }
    
    [HttpGet("api-usage")]
    public async Task<ActionResult<ApiUsageDto>> GetApiUsage([FromQuery] DateTime? startDate, [FromQuery] DateTime? endDate)
    {
        // Return API usage statistics
        return Ok(new ApiUsageDto
        {
            TotalRequests = 50000,
            AverageResponseTime = 250,
            ErrorRate = 0.1,
            MostUsedEndpoints = new List<EndpointUsageDto>
            {
                new() { Endpoint = "/api/orders", Requests = 15000, AvgResponseTime = 200 },
                new() { Endpoint = "/api/products", Requests = 12000, AvgResponseTime = 150 },
                new() { Endpoint = "/api/inventory", Requests = 8000, AvgResponseTime = 300 }
            }
        });
    }
}

================================================================================
TỔNG KẾT HOÀN CHỈNH
================================================================================

🎯 **IMPLEMENTATION ROADMAP CHI TIẾT:**

**WEEK 1-2: CORE SETUP**
✅ Project structure setup với Clean Architecture
✅ Database design và Entity Framework configuration
✅ JWT Authentication và Authorization system
✅ Basic CRUD operations cho core entities
✅ API documentation với Swagger
✅ Unit testing framework setup

**WEEK 3-4: CORE BUSINESS LOGIC**
✅ POS system implementation với real-time features
✅ Order processing và payment handling
✅ Inventory management với stock tracking
✅ File upload system cho images và documents
✅ SignalR implementation cho notifications
✅ Basic analytics và reporting

**WEEK 5-6: ADVANCED MODULES**
✅ CRM module với customer management
✅ LRM module với supplier và logistics
✅ HRM module với employee management
✅ Advanced analytics và dashboard
✅ Email notifications system
✅ Report generation với Excel export

**WEEK 7-8: OPTIMIZATION & DEPLOYMENT**
✅ Performance optimization với Redis caching
✅ Production configuration và security hardening
✅ Docker containerization
✅ CI/CD pipeline setup
✅ Monitoring và logging implementation
✅ Load testing và performance tuning

**WEEK 9-10: INTEGRATION & TESTING**
✅ Frontend-backend integration testing
✅ End-to-end testing scenarios
✅ Security testing và penetration testing
✅ User acceptance testing
✅ Production deployment
✅ Documentation completion

🔧 **COMPLETE DEVELOPMENT STACK:**

**Backend Technologies:**
- C# ASP.NET Core 8.0 Web API
- Entity Framework Core 8.0
- SQL Server 2022 / PostgreSQL 15
- Redis 7.0 cho caching
- SignalR cho real-time features
- JWT cho authentication
- AutoMapper cho object mapping
- FluentValidation cho input validation
- Serilog cho structured logging
- Swagger/OpenAPI cho documentation

**Infrastructure:**
- Docker containers
- NGINX reverse proxy
- Azure App Service / AWS ECS
- Azure SQL Database / AWS RDS
- Azure Redis Cache / AWS ElastiCache
- Azure Application Insights / AWS CloudWatch
- Azure Storage / AWS S3 cho file storage

**Development Tools:**
- Visual Studio 2022 hoặc VS Code
- SQL Server Management Studio
- Redis Desktop Manager
- Postman hoặc Swagger UI
- Docker Desktop
- Azure DevOps / GitHub Actions

📊 **PERFORMANCE TARGETS:**
- API response time: < 200ms (95th percentile)
- Database query time: < 100ms average
- File upload: Support up to 10MB files
- Concurrent users: Support 1000+ concurrent connections
- Real-time notifications: < 100ms latency
- Cache hit ratio: > 85%
- System uptime: 99.9% availability

🔒 **SECURITY IMPLEMENTATIONS:**
- JWT token validation với refresh tokens
- Role-based permissions với granular control
- Input validation và sanitization
- SQL injection protection via EF Core
- File upload security với type và size validation
- CORS configuration cho frontend domains
- HTTPS enforcement với SSL certificates
- Rate limiting cho API endpoints
- Audit logging cho all user actions

🚀 **DEPLOYMENT CHECKLIST:**
✅ Environment configuration files
✅ Database migration scripts
✅ SSL certificate installation
✅ Load balancer configuration
✅ Monitoring và alerting setup
✅ Backup và disaster recovery plan
✅ Performance monitoring dashboard
✅ Error tracking và notification system
✅ API documentation deployment
✅ Health check endpoints

📚 **COMPLETE API COVERAGE:**
- 70+ API endpoints covering all frontend requirements
- Authentication và user management (8 endpoints)
- Product và inventory management (15 endpoints)
- Order processing và payments (12 endpoints)
- Analytics và reporting (8 endpoints)
- CRM functionality (11 endpoints)
- LRM logistics management (10 endpoints)
- HRM human resources (13 endpoints)
- File management và utilities (6 endpoints)
- Real-time notifications (SignalR hubs)

🔄 **MAINTENANCE & SUPPORT:**
- Automated database backups
- Log rotation và archival
- Performance monitoring alerts
- Security patch management
- Feature flag implementation
- A/B testing capabilities
- Blue-green deployment strategy
- Rollback procedures
- Documentation maintenance
- Team knowledge transfer

Tài liệu này hiện đã hoàn chỉnh với tổng cộng hơn 20 sections chi tiết, cung cấp roadmap đầy đủ cho team backend C# để implement một hệ thống POS API production-ready, tương thích 100% với frontend React hiện có. Mọi aspect từ development đến deployment đều được covered chi tiết với code examples, best practices, và troubleshooting guides.

================================================================================
                           END OF COMPLETE GUIDE
================================================================================